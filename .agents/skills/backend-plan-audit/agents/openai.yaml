interface:
  display_name: "Backend Plan Audit"
  short_description: "Audit all active or explicitly selected plans"
  default_prompt: "Use $backend-plan-audit to audit all active implementation plans."
policy:
  allow_implicit_invocation: false
