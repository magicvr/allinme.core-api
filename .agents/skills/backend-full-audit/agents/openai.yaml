interface:
  display_name: "Backend Full Audit"
  short_description: "Run a traceable full-repository backend audit"
  default_prompt: "Use $backend-full-audit to perform a complete repository audit."
policy:
  allow_implicit_invocation: false
