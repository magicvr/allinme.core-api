interface:
  display_name: "Backend Plan Acceptance Audit"
  short_description: "Independently verify plan implementation readiness"
  default_prompt: "Create a new runtime task, then use $backend-plan-acceptance-audit there to verify each active plan's readiness."
policy:
  allow_implicit_invocation: false
