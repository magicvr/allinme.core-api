interface:
  display_name: "Backend Plan Acceptance Audit"
  short_description: "Independently verify plan implementation readiness"
  default_prompt: "Use $backend-plan-acceptance-audit in a new execution context to verify each active plan's readiness."
policy:
  allow_implicit_invocation: false
