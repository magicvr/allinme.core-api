interface:
  display_name: "Follow-up Audit"
  short_description: "Verify each REM in a separate execution context"
  default_prompt: "From a parent task, create a child and use $backend-follow-up-audit with its runtime CONTEXT_REF to verify a pending REM."
policy:
  allow_implicit_invocation: false
