interface:
  display_name: "Plan Audit Until Ready"
  short_description: "Run set-aware plan audit closure with verification-first routing"
  default_prompt: "Use $backend-plan-audit-until-ready to reach ready with persisted peer snapshots, exact-revision evidence, and runtime-ref child audits."
policy:
  allow_implicit_invocation: false
