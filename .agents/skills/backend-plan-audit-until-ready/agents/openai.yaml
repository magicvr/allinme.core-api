interface:
  display_name: "Plan Audit Until Ready"
  short_description: "Audit and remediate plans until implementation-ready"
  default_prompt: "Use $backend-plan-audit-until-ready to audit active plans until readiness acceptance passes."
policy:
  allow_implicit_invocation: false
