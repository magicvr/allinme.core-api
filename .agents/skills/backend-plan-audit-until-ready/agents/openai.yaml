interface:
  display_name: "Plan Audit Until Ready"
  short_description: "Close plan findings with independent acceptance"
  default_prompt: "Use $backend-plan-audit-until-ready to close active plan audit chains with separate-context review and acceptance."
policy:
  allow_implicit_invocation: false
