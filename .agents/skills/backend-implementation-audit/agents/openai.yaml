interface:
  display_name: "Backend Implementation Audit"
  short_description: "Independently audit exact implementation revisions"
  default_prompt: "Create a new runtime task, then use $backend-implementation-audit there to audit each completed IMP at its exact result revision."
policy:
  allow_implicit_invocation: false
