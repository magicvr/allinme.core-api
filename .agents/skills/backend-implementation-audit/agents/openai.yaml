interface:
  display_name: "Backend Implementation Audit"
  short_description: "Create one implementation audit per IMP"
  default_prompt: "Use $backend-implementation-audit to create or resume one formal audit for each completed IMP."
policy:
  allow_implicit_invocation: false
