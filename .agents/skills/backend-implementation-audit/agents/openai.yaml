interface:
  display_name: "Backend Implementation Audit"
  short_description: "Independently audit exact implementation revisions"
  default_prompt: "From a parent task, create a child and use $backend-implementation-audit with its runtime CONTEXT_REF at the exact IMP revision."
policy:
  allow_implicit_invocation: false
