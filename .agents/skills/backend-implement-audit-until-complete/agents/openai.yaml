interface:
  display_name: "Implement and Audit Until Complete"
  short_description: "Implement and audit selected plans to complete"
  default_prompt: "Use $backend-implement-audit-until-complete to implement selected plans with set-aware readiness, verification-first remediation, and separate-context completion acceptance."
policy:
  allow_implicit_invocation: false
