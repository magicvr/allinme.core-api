interface:
  display_name: "Backend Implementation Acceptance Audit"
  short_description: "Independently verify implementation completion"
  default_prompt: "Use $backend-implementation-acceptance-audit in a new execution context to verify completion from the IMP/REM revision chain."
policy:
  allow_implicit_invocation: false
