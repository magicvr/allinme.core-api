interface:
  display_name: "Backend Implementation Acceptance Audit"
  short_description: "Independently verify implementation completion"
  default_prompt: "From a parent task, create a child and use $backend-implementation-acceptance-audit with its runtime CONTEXT_REF to verify completion."
policy:
  allow_implicit_invocation: false
