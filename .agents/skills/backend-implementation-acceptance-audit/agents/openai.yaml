interface:
  display_name: "Backend Implementation Acceptance Audit"
  short_description: "Independently verify implementation completion"
  default_prompt: "Create a new runtime task, then use $backend-implementation-acceptance-audit there to verify completion from the IMP/REM revision chain."
policy:
  allow_implicit_invocation: false
