---
id: GOAL-001-allinme-core-api
doc: decision
status: blocked
parent: null
created: 2026-07-23
updated: 2026-07-23
version: 0.2.0
---

# 决策记录 · GOAL-001

## 信息需求与阶段门禁

权威表见 [00-meta.md](00-meta.md)。

- **I-001** 策略已 `decided`（A 协议演进）。
- **I-006** 新协议制品 `collecting`：**关闭前** Root 与 R1 保持 `blocked`，不得恢复实施。
- I-002 / I-003 暂停主动推进至 I-006 关闭。

## D-001 · 总目的与交付边界

**决定**：

- 本项目是**可复用核心 API 基座** + **Schema-UI 后端实现方**（page 生产、业务 API、鉴权等）。
- **仅后端**交付；Admin 前端 / Renderer 在其他仓库。
- 复用策略：**模板/骨架复用为主**，可复用能力**逐步**抽到 `pkg/`（轻量 C），不追求一开始就是完整框架库。

**为什么**：

- 协议仓库章程明确：不提供业务后端；本仓补齐「后端生产方」角色。
- 前端与 Renderer 分仓可避免协议/渲染/业务后端三者耦合，便于多项目复用同一后端基座。
- 过早抽大而全库会拖慢 MVP；先在骨架内跑通再沉淀 `pkg/` 更稳。

**未选方案**：

- **本仓同时做 Renderer/Admin 壳**：扩大范围，偏离「核心 API」焦点。
- **仅做成不可 fork 的一次性 demo**：无法支撑「后续项目复用」。
- **一开始做成完整 Go framework 库**：设计成本高，信息不足时易空转。

## D-002 · Schema-UI 协议版本钉死（修订中）

**原决定（立项时）**：

固定消费协议制品 **`2.0.0`**（`meta.protocolVersion: "2.0"`）；升级须单独决策。

**修订状态（2026-07-23 · D-005）**：

- `2.0.0` 仍为**当前对照基线**（用于记录缺口，非最终实施钉死）。
- 因批量等能力选择协议演进，**实施将改钉新协议版本**；新版本号/tag/SHA-256 在 I-006 `verified` 时由**新的钉死决策**写入（不得静默跟进 HEAD）。
- 原则不变：只消费不可变制品；不在本仓扩展协议语义。

**为什么保留「必须钉死」原则**：前后端同一制品、可验收、可复现。

**未选方案**：

- **不钉版本、随文档 HEAD 漂移**：不可复现。
- **在本仓复制并修改协议副本**：违反权威层级。

## D-003 · 第一阶段以 MVP Demo Admin 推进

**决定**：

路线图 R1 对应 `GOAL-002-mvp-demo-admin`：demo 完整 Admin（真实登录/权限；schema 菜单路由；订单/钱包/通知；仪表盘；行内+批量）。验收：page schema 覆盖 Admin 全部入口。批量走协议演进（见 D-005）。

**为什么**：用户确认的最小可验证方向；P-001 先路线图再分子目标。

**未选方案**：

- **先只做 ping/health 无业务域**：无法验证 Admin 协议闭环。
- **同时铺开生产级多租户/计费等**：超出 demo 范围。

## D-004 · 工作区与代码布局（默认确认）

**决定**：

- 治理真相源：仓库根平铺 `GOAL-*` + `goal-tree.md`（隐式单工作区）。
- 代码布局：维持现有 **仓库根 Go 布局**（`cmd/`、`internal/`、`pkg/`）。

**为什么**：与当前仓库骨架一致。

## D-005 · 协议优先演进；本仓暂停

**决定（2026-07-23 · 用户确认）**：

1. 批量动作交付策略采用 **A：推动 `schema-ui-docs` 协议演进并发布新版本**（不选 Host Extension、不选 MVP 去掉批量）。
2. **`GOAL-001` 与 `GOAL-002` 均 `status: blocked`**；本仓暂停 R1 方案细化与实现。
3. 协议工作在 **`schema-ui-docs` 仓库**进行；本仓不伪造协议字段/语义。
4. **恢复条件**：新协议制品可固定引用且能力覆盖 MVP 需求（至少批量核心协议支持；CRUD 相关缺口有协议支持或书面 residual）→ 关闭 I-006 / GOAL-002 I-008 → 新决策钉死协议版本 → 用户确认恢复 `/govern`。

**为什么**：互操作批量必须进协议；用户接受本项目等待协议完成。

**未选方案**：本仓边做边等、或私自扩展 page 契约。
