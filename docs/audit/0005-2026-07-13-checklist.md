---
status: active
owner: 后端团队
created: 2026-07-13
last_updated: 2026-07-14
applies_to: implementation roadmap phase 5 attachments
---

# 阶段五：附件生命周期实施 Checklist

配套计划：[阶段五附件生命周期开发计划](./0005-2026-07-13-plan.md)。除非条目按下列固定模板记录实际 Evidence，否则不得勾选；真实进程、跨平台、ENOSPC 和部署证据必须使用全局唯一 run ID，最终报告必须引用这些 run ID。

每个勾选项的 Evidence 固定记录：`Owner`、`Run ID`、`Date/Timezone`、`Revision`、`Environment`、`Command`、`Artifact/Log Path`、`Result`、`Failure/Not-Run Reason`。不适用字段写 `N/A` 并说明原因，禁止只写“已验证”或只引用第三方审计结论。

## P0. 契约与基线

- [ ] P0-1. 在任何 schema/API 编码前提交独立事实源变更，逐项同步 `docs/05-domain-model.md`、`docs/03-http-api-target.md`、`docs/06-implementation-roadmap.md`、`docs/04-validation.md` 和 plan：阶段五只交付订单删除 cleanup 原语与测试、不新增订单删除或页面 endpoint；目标 API 改为显式允许角色；领域模型补附件状态、上限、owner/expiry/绑定和下载业务语义。Evidence 必须记录每处替换文本、行号和包含该变更的准确 revision；该 revision 前保持 No-Go。
- [ ] P0-2. 产出可执行 schema v7 migration/trigger 草案：使用独立 `order_attachments(order_id, attachment_id, position, bound_at)`，DDL 只承诺枚举、0-based position、BLOB SHA-256、业务外键 RESTRICT、映射唯一性和恢复索引；journal 的 `group.order_id`、`op.original_order_id` 与历史 `op.attachment_id` 不使用 CASCADE/RESTRICT 外键，作为恢复快照/墓碑保留到 purge，靠 BUILDING→PREPARED trigger、最终事务 token 校验和启动扫描保证一致性。repository 在事务提交前验证状态/映射 invariant；转相 trigger 验证 expected_count、组/明细 kind、普通 operation 附件引用，repository 验证派生 basename。覆盖 2 项交换、3 项循环、满 10 项逆序、remove+add+reorder、失败回滚保持原顺序、损坏行以及“订单已删除但 COMMITTED tombstone 仍存在”fixture，并记录 `artifacts/phase5/v7/<revision>/evidence/` 路径。
- [ ] P0-3. 冻结 `att_` ID、10 MiB 文件上限、multipart 总上限、增量单一 `file` part、生产 30 秒 read deadline、PDF/PNG/JPEG 探测、缺失/octet-stream/不一致声明 MIME；从原始 Content-Disposition 严格解析唯一 `filename` 或 UTF-8 `filename*`，先 NFC 规范化，拒绝重复/并存参数、RFC 2231/percent 错误、Windows/Linux 路径、所有 Unicode `Cc` 和双向 override/isolate 字符。冻结 `filename*` RFC 5987 编码与 ASCII fallback 的保留/折叠/空值/120-byte 截断 fixture，禁止以 `Part.FileName()` 作为安全输入。所有现有与新增 ResponseWriter 包装器实现 `Unwrap()`；短 duration 的真实 `httptest.Server` 证明 `SetReadDeadline` 到达真实连接并触发 `408`，装配测试另断言生产值精确为 30 秒。若引入 `golang.org/x/text/unicode/norm` 等依赖，记录版本、`go mod tidy`、模块 diff 与许可证/供应链检查，不自制 Unicode 规范化。
- [ ] P0-4. 冻结 STAGING 与组 BUILDING/PREPARED/COMMITTED recovery：每个 DB commit/rename 边界、operation ownership/CAS、temp/final/trash/orphan 命名、完整 source/trash 真值表、整组 restore/finalize/purge、启动恢复、readiness 和失败重试责任；在 `internal/testkit/phase5` 或等价受审计目录交付可版本化 crash harness/fixture/脚本，固定边界名、退出方式、Windows/Linux 运行方式、重启命令、预期 DB/文件状态、ENOSPC 注入、监督器/调度 profile 和 Evidence 路径，供 M1A-10/M1B-5/M4-9 复用；明确只保证进程退出恢复，不宣称主机掉电安全。
- [ ] P0-5. 冻结 upload/download/delete 的角色、短路顺序、精确错误矩阵、成功 DTO、`url` 禁止项、200/304 header/body 矩阵、ETag/If-None-Match、条件请求前完整性校验、`Range` 忽略并完整返回 200、`Accept-Ranges: none`、HEAD=405、响应提交后传输失败和真实 server 可观察行为。下载在线性化点“BOUND 映射 + 订单授权 + 身份匹配句柄已打开”后允许完成，线性化点前观察到 REMOVING 才返回 503。
- [ ] P0-6. 冻结 order create/edit 的 `attachmentIds` 缺失/空数组/顺序/最终集合语义，以及 owner、expiry、绑定状态和不可见性错误规则。5A PATCH 不开放该字段：省略必须保留映射和顺序，显式传入按未知字段返回 400，普通 PATCH 响应仍带完整附件摘要；5B 才启用完整集合语义。
- [ ] P0-7. 冻结幂等精确语句顺序：lookup/replay → 事务外文件事实校验 → 预构造完整 v2 projection/snapshot → 写事务第一条执行 `INSERT INTO idempotency_keys (...) VALUES (...) ON CONFLICT (principal_user_id, method, route, idempotency_key) DO NOTHING` → `RowsAffected=0` 的 loser 立即读取 winner → 仅 winner 校验 owner/state/expiry 并创建。事务外读取/hash/type/身份/projection 的每个失败出口必须在返回文件错误前按同一 scope/key 再查 winner；late winner 已出现时按其 snapshot version/digest 重放或冲突。首写行必须一次填满现有 NOT NULL 字段并成为最终可重放 snapshot，不引入 pending 状态或二次保存 snapshot；其他约束错误必须返回并回滚。冻结 5 秒 SQLite busy/winner 读取预算、winner 回滚后 loser 可重试成为 winner、digest v2 canonical JSON、v1 key + 非空附件冲突、未知版本 internal 和双连接固定 fixture。
- [ ] P0-8. 冻结 snapshot v2 的完整首次 Order response projection 与 `CreateResult` service/handler 边界；handler 不按重放时角色重算 capability。冻结列表 `attachmentCount` 与 detail/write/Action `attachments`，v1 默认字段和 v1/v2 均不查询当前附件。
- [ ] P0-9. 冻结 edit 真实协议与 REMOVING 窗口矩阵：准备事务 → 文件隔离 → 最终订单事务 → purge；窗口内列表/详情/Action 保留旧摘要、count 计入、下载 503、其他写允许竞争，最终事务重验 version/refund/group，失败整组 restore。
- [ ] P0-10. 冻结运行期 cleanup 不接管其他 owner 的 STAGING/REMOVING/PREPARED，只处理稳定过期、自己领取的 operation、COMMITTED、temp/orphan；遗留 PREPARED 仅由对外服务前的独占启动恢复处理。真实双进程测试证明 cleanup 跳过该 operation，旧 owner 仅在 token/phase 仍匹配时可继续，变化后立即停止。
- [ ] P0-11. 冻结 CORS：Allow-Methods 包含 DELETE，Expose-Headers 精确包含 `X-Request-ID, Content-Disposition, ETag, Accept-Ranges`，200/304/JSON error 均覆盖，禁用附件路由后 preflight 回到 404。
- [ ] P0-12. 冻结下载 8 槽进程级 semaphore、最多 80 MiB 校验缓冲预算、超限 503/Retry-After、取消/关闭释放，以及持久损坏 500、临时 I/O/锁/恢复中 503、不可见 404、本人 BOUND 删除 409、上传超时 408 的错误矩阵。
- [ ] P0-13. 完成 stable applock 最小原型：冻结可复用 `Acquire(path, Mode)`/释放 API、显式 Shared/Exclusive、Unix 持久锁文件 + flock、Windows 持久 handle + LockFileEx；先迁移现有 API/reset/seed/migrate/cleanup/bootstrap-admin 入口，再允许附件恢复依赖 maintenance lock。冻结 API 在打开 DB/root 前取得 `API exclusive -> maintenance exclusive`，migrate/reset/seed 要求停 API 并取两把独占锁，cleanup 取 maintenance shared，bootstrap-admin 取 maintenance exclusive，busy 分类稳定且 Close 不删除锁文件。真实三进程 close/reacquire 以及完整入口互斥矩阵通过后，才能使用 maintenance lock 设计。
- [ ] P0-14. 在 plan/checklist 进入版本库后的准确 revision 记录 `git status --short`、Go 版本、`go list -m all`、`go test ./... -count=1`、`go vet ./...`、`go test -race ./... -count=1`、文档 validator 和 `git diff --check` 基线；若新增依赖，另记录 `go mod tidy` 前后 diff、版本和许可证/供应链检查。未执行项和工作树差异必须显式记录，不得把 `e28e9ac` 当作包含本计划的 revision。
- [ ] P0-15. 完成文件安全最小原型：Go 1.26 `os.Root` 持久根句柄、服务账号独占写权限假设、Unix dev/inode 与 Windows volume/file ID、hash 后重新打开身份比较、同卷 rename/目标不存在/no-follow 语义；真实 symlink/reparse 交换竞态通过。Windows/Linux 分别覆盖“读句柄保持打开 → 并发 final→trash rename → 原句柄继续读”，冻结 Windows `CreateFile` share mode；若 Windows 不能 rename，冻结 edit-remove unavailable/重试与 PREPARED 保留语义。原型不能证明安全时先修改威胁模型或平台适配方案，M1A 不开工。
- [ ] P0-16. P0 原型完成后重新估算平台适配、crash harness、锁协议和 5A 部署成本；记录 ROM 与新基线差异。P0 的 6–10 天只在无自定义 syscall 且跨平台原型一次通过时成立；若需要专用 Windows/Unix 打开/rename 层，P0 与 M1A 分别增加缓冲，不压缩恢复或 race 门禁。
- [ ] P0-17. 冻结应用驱动运行契约：生产 watchdog 每 5 秒用可注入 clock/ticker 检查 unresolved；严格超过 5 分钟后最多一个 tick 内 `/readyz` not-ready，并只调用一次 shutdown callback；一次性 shutdown-reason arbiter/typed `RunResult` 把原因从 app 传播到 `cmd/api`，优先级为启动前 schema gate 78 > 监听/运行失败 1 > watchdog 75 > 外部 SIGTERM/普通 Close 0，重复原因幂等且较低优先级不能覆盖。停止接收新请求、在途请求宽限 15 秒后按选定原因退出，无 probe 流量也生效；watchdog 不执行 cleanup。冻结外部调度 profile：`cleanup-attachments` 每 15 分钟、超时 10 分钟、禁止重叠、非零退出告警，并记录真实子进程竞争测试和受支持的监督/调度示例。
- [ ] P0-18. 冻结启动扫描边界：journal 恢复后按 500 行批次同步检查 schema、group/op、状态/映射计数、basename、final 存在性、普通文件身份和记录 size；启动不全量 hash/type。先冻结至少一套支持规模（附件数、映射数、operation 数、平均 metadata 成本）和可生成 fixture；相同记录数、不同文件大小下分别记录冷/热缓存 wall-clock，证明成本按记录/metadata 操作增长而不按总字节读取。30 秒为硬上限，是否同时作为 SLO 以实测结论为准；超时 fail-closed，并记录诊断码、日志路径和离线 `verify-attachments --full`/恢复步骤。
- [ ] P0-19. 冻结 v6 gate、v7 schema-only、v7 5A feature 三产物发布矩阵和完整命令链：schema-only admin 的 `migrate` 在业务装配前允许 v6→v7/幂等 v7 校验，其他 admin 子命令与 API 只允许 v7；`migrate` 成功退出 0、非 v6/v7 退出 78、兼容性校验或执行失败退出 1。逐步记录允许 schema/data、失败恢复点和 binary SHA-256。每个 binary 输出机器可验证 capability manifest（artifact kind、schema、允许数据阶段），部署 validator 核对 manifest/SHA/schema scan，并覆盖“错误 binary + 错误 schema/数据”负向 smoke；文件名仅作辅助。schema-only gate 使用固定 SQL 扫描四张附件表与 snapshot v2；构造入口返回同一 typed error，真实 binary 映射退出 78。“同一候选 revision”只约束 5A feature binary。
- [ ] P0-20. 冻结统一 startup coordinator/typed gate API 与入口阶段矩阵：`NewAPI`、`NewAuthenticatedAPI`、`Handler()`、embedded entry、migrate、seed、reset、cleanup、bootstrap-admin 都必须经过同一 owning abstraction，明确锁、DB/root、schema gate、恢复、扫描、service/handler 构造的顺序和每个入口允许停在哪一步；测试装配不得旁路。
- [ ] P0-21. 把 P0 分成五个独立工作包：事实源同步、v7 migration/trigger/journal 草案、stable applock 与入口迁移、Windows/Linux files 原型、crash/release harness 与监督/调度 profile。每包记录 owner、输入依赖、出口条件、最大 timebox、revision 和 Evidence；全部完成后重新估算 M1A/M2/M3A/5A-D/M1B/M3B/M4/5B，记录 review 带宽、环境可用性、关键路径与资源不足降级顺序，重估前不承诺日期。
- [ ] P0-22. 明确部署支持矩阵：至少选择并实测一个单机 profile（Windows Service/Task Scheduler 或 Linux systemd/timer）；未完成监督、cleanup 调度、退出码和恢复证据的平台只能标记为开发/CI 兼容，5A-D 批准范围不得超出已验证 profile。
- [ ] P0-23. 交付机器可解析的 Evidence/manifest 最小工具链（固定 JSON schema、全局唯一 run ID、Owner、Date/Timezone、Revision、Environment、Command、Artifact/Log Path、Result、Failure/Not-Run Reason、artifact kind），validator 必须核对 revision、SHA-256、退出码、路径和 manifest/run 关联；P0-14、P0-19、P0-22 各产生至少一份真实样例及 validator 输出。未通过 validator 或仅有 Markdown/第三方报告时不得勾选任何对应完成项。
- [ ] P0-24. 建立协议变更清单，覆盖 P0-1、P0-2、P0-13、P0-17、P0-19、P0-20；任何事务顺序、状态、DTO、错误码、锁/启动语义或平台能力变化，必须在同一 revision 同步更新 plan/checklist/事实源/Evidence 模板，记录受影响里程碑并重跑相关 gate，禁止局部放宽门禁。

## M1A. 5A Schema、文件适配器与单项恢复

- [ ] M1A-1. 按 P0-19 先交付不可变 v6 gate binary，在打开业务 service、构造 handler 或监听前强制 schema gate；记录完整 revision/命令/文件/SHA-256，真实进程证明 `user_version != 6` 固定退出 78。停服并完成成套备份后，由 schema-only admin 的专用 `migrate` 路径在业务装配前执行 additive v6→v7 migration；该路径只允许 v6/v7，其他 schema、非预期旧附件表/数据必须 typed failure 并回滚。migration 创建 `attachments`、`order_attachments`、最终 group/op 表与必要索引，覆盖空库、标准 v6、重复 migration、失败恢复点、绕过/损坏行、旧业务数据保留以及 COMMITTED tombstone 跨订单删除保留。
- [ ] M1A-2. 在任何附件路由、Order DTO 或 digest v2 行为变化前，按发布矩阵构建 schema-only `api/admin` artifact；记录完整 revision、干净构建命令、文件名、Go 版本、SHA-256、数据库路径和恢复点。先用同一 admin SHA 执行 `migrate`，再让 API/其他 admin 子命令通过共用构造期 gate 验证 v7 空数据条件；任一附件行、snapshot v2 或 schema 非 v7 时返回同一 typed error/退出 78。capability validator 与错误 binary/schema 负向 smoke 必须通过；该 artifact 明确不是功能启用后的回退版本。
- [ ] M1A-3. `internal/files` 只接受启动时打开的 `os.Root` 和服务端生成键；实现普通文件句柄身份验证、同卷 temp/final/trash rename、目标不存在、no-follow/reparse/symlink 防线。覆盖路径逃逸、非普通文件、权限失败、hash 后身份替换和真实交换竞态；部署文档冻结根目录独占写权限。
- [ ] M1A-4. 流式写入严格限制 10 MiB，计算 SHA-256，探测 PDF/PNG/JPEG，校验 PNG/JPEG header 和 PDF 签名；正常协议/校验失败立即清理 temp，真实进程退出或 sync/rename/DB 边界失败只允许留下可由 attachment ID 识别的 temp，匿名、越界或无法绑定 ID 的 temp 必须清理/隔离。
- [ ] M1A-5. 只实现 5A 所需 STAGING、单项 DELETE_UNBOUND/EXPIRE_UNBOUND 的 PREPARED/COMMITTED、final rename、trash 隔离、restore 和 purge；所有 commit/rename 边界可失败注入，且留下可分类、可重试、不可下载的持久状态。5A binary 遇到 group/EDIT_REMOVE/ORDER_DELETE/ORPHAN_FINAL 状态时在构造 handler 前 fail-closed。
- [ ] M1A-6. 附件 repository 覆盖 STAGING 创建/晋级、`order_attachments` 批量读取、owner/state/expiry CAS 绑定、单项 operation ownership、状态恢复、过期批次和损坏行分类；提交前验证状态/映射 invariant，不得逐附件查询。
- [ ] M1A-7. 启动按 500 行批次和 30 秒硬上限交叉验证文件元数据、映射、basename、final 存在性、普通文件身份和记录 size，不在启动路径全量计算 SHA-256/type；稳定 UPLOADED/BOUND 缺失、不安全或 size 不一致时阻止启动，超预算 fail-closed。按 P0-18 的支持规模生成 fixture，记录相同记录数、不同文件大小的冷/热缓存 wall-clock、诊断码和离线修复命令，并覆盖单项 PREPARED/COMMITTED 的 final/trash 真值组合。
- [ ] M1A-8. API 在打开 DB/root、schema gate、恢复和 service/handler 构造期间持有两把锁；`Handler()` 测试与嵌入式入口不能绕过恢复。两个独立进程竞争 bind/delete/minimal-cleanup 时至多一个成功，并重跑完整 admin 锁矩阵。
- [ ] M1A-9. `cmd/admin cleanup-attachments` 的 5A 版本处理过期 UPLOADED、5A 单项 COMMITTED 和旧可识别 temp，跳过其他 owner PREPARED；输出稳定摘要，重复执行收敛。API 实现 P0-17 watchdog：每 5 秒检查、超过 5 分钟后最多一个 tick 内 not-ready、一次性 shutdown callback、15 秒宽限、退出码 75、重复触发幂等、无 probe 流量真实子进程可验证，且绝不在线接管 PREPARED 或执行 cleanup。
- [ ] M1A-10. 使用 P0-4 固定 crash harness，在 STAGING commit、temp→final、UPLOADED commit、单项 ownership commit、final→trash、COMMITTED 和 purge 的每个边界强制进程退出；重启恢复必须收敛为稳定 UPLOADED/BOUND、完整 restore 或不可下载 purge，函数级 failure injection 不得替代真实退出证据。
- [ ] M1A-11. 单独验证旧 binary 回退只能在停写并恢复 DB/WAL/SHM 与附件目录完整备份后执行，记录 hash、命令和数据丢失边界；未经 schema gate 的历史 v6 binary 标记为禁止部署。
- [ ] M1A-12. `go test ./internal/files ./internal/store ./internal/admin ./internal/app -count=1` 与对应非缓存 race 门禁通过。

## M2. 附件 HTTP 闭环

- [ ] M2-1. `POST /api/v1/attachments` 只允许 operator/admin，严格执行 CORS → auth → role → multipart → body limit/read deadline → 增量 part + 原始 Content-Disposition → 可识别 temp 流式写入 → EOF 结构确认 → STAGING → rename → UPLOADED；tracking ResponseWriter 实现 `Unwrap()`，短 duration 的真实慢速 server 断言 deadline 穿透并返回 408，独立装配测试断言生产 duration 精确为 30 秒；成功返回冻结 201 JSON，明确无任意 URL 字段。
- [ ] M2-2. 上传拒绝错误媒体类型、缺失/非法 boundary、重复 file、额外字段、多文件、危险文件名、空文件、超限、显式声明 MIME 与内容不一致和非法内容；原始扩展名不决定允许类型，存储扩展名必须由探测结果派生。所有正常协议/校验失败断言无可见 DB row、不可识别 final、匿名 temp 或可识别 temp 残留。
- [ ] M2-3. 上传在 temp create/write/sync/close、final rename、DB insert/commit 和补偿清理各失败点稳定返回安全错误；只有真实进程退出或 sync/rename/DB 边界失败可保留可识别 temp 供启动恢复，匿名/越界 temp 必须清理或隔离；响应与日志不出现绝对路径或客户端敏感内容。
- [ ] M2-4. `GET /api/v1/attachments/{attachmentId}` 只下载已绑定且订单可查看的附件；四角色真实 token 成功，未认证 401，不可见/不存在/非法 ID 为 404，未绑定为 404。
- [ ] M2-5. 用真实 `httptest.Server` 验证 200/304 header/body 矩阵：NFC 后的 RFC 5987 `filename*`、只保留 `[A-Za-z0-9._-]` 且折叠/空值/120-byte 截断确定的 ASCII fallback、强 ETag、`Accept-Ranges: none`、nosniff/private,no-store、Content-Length/Content-Type 的存在与缺失；覆盖 If-None-Match 列表/`*`/弱标签、HEAD=405，以及合法/非法/多段 Range 都在完整性校验后被忽略并完整返回 200。
- [ ] M2-6. 下载通过 service 层单一入口完成 BOUND 映射读取、订单授权、安全打开、身份复核和 8 槽 semaphore，在提交响应前完成有界 size/hash/type 校验，handler 不得拼接可绕过顺序的 repository 调用；在线性化点后并发 REMOVING 允许在途下载完成，之前观察到 REMOVING 返回 503。Windows/Linux 真实句柄测试覆盖“保持读句柄 → final→trash rename → 原句柄继续读”并断言 Windows share mode；若平台不允许 rename，edit-remove 稳定 unavailable 且 PREPARED 可重试。槽满/临时 open-read 为 503 + Retry-After，永久缺失/不一致为 500，304 不掩盖损坏；覆盖并发下载、取消、关闭和响应提交后写失败时资源释放与安全日志。
- [ ] M2-7. `DELETE /api/v1/attachments/{attachmentId}` 只允许 operator/admin 删除本人 `UPLOADED`；成功 204，本人已绑定 409，他人/不存在/非法 ID 404，重复删除 404。
- [ ] M2-8. route metadata、mux 和 CORS 使用同一附件路由集合；Allow-Methods 含 DELETE，Expose-Headers 精确，200/304/JSON error actual CORS 一致；合法 preflight 204，错误 requested method/origin 稳定拒绝，unknown path 404，Allow 精确。
- [ ] M2-9. 设置 `DisableAttachmentRoutes` 后 upload/download/delete 全部 404，订单、退款和看板仍可用；字段不从运行环境加载。
- [ ] M2-10. `go test ./internal/files ./internal/store ./internal/httpapi ./internal/app -count=1` 与对应非缓存 race 门禁通过。

## M3A. Create 绑定与幂等兼容

- [ ] M3A-1. Order create command/HTTP 输入支持唯一、有序、最多 10 个 `attachmentIds`；缺失与空数组等价，未知字段和非法 ID 仍走严格 JSON/validation 门禁。
- [ ] M3A-2. create 先按 scope/key lookup 和 snapshot version 短路；miss 时事务外只做文件事实校验并预构造完整 v2 projection/snapshot，所有读取/hash/type/身份/projection 失败出口在返回文件错误前再次 lookup late winner。写事务第一条按 P0-7 的显式 `ON CONFLICT (...) DO NOTHING` SQL 插入最终 snapshot reservation；loser 依据 `RowsAffected=0` 立即读取 winner，不再验证附件；只有 winner 才验证 owner/state/expiry/轻量身份、创建订单/items、写 `order_attachments`、CAS BOUND 并与 reservation 一起提交，不再二次保存 projection。覆盖其他约束错误回滚、winner 后续失败整体回滚、loser 重试成为 winner 和 5 秒 busy 预算耗尽的稳定 503。
- [ ] M3A-3. canonical digest v2 固定字段顺序 `operation/customerName/currency/items/attachmentIds`，缺失/空数组编码为 `[]`；同 key 同事实重放、JSON 布局/字段空白规则和 attachmentIds 顺序差异冲突均有固定字节 fixture。
- [ ] M3A-4. 历史 snapshot/digest v1 fixture 原样可读可重放，返回 `attachmentCount=0` 和空 attachments 且不查询当前附件；v2 `CreateResult` 返回首次冻结 projection，handler 不重算 capability；未知版本或损坏 JSON/digest 返回 internal。
- [ ] M3A-5. 绑定拒绝他人、过期、删除中、已绑定其他订单、重复 ID、超过数量和损坏文件；错误不泄露附件是否属于其他用户。
- [ ] M3A-6. 订单列表批量返回 `attachmentCount`，详情/create/Action 返回按 position 排序的摘要；不存在 N+1，字段不含路径、存储键和内部 owner ID。
- [ ] M3A-7. 两个独立连接覆盖查询无幂等记录 → 文件校验 → reservation winner 创建 → loser `RowsAffected=0` 后按 winner v2 重放、winner 回滚后 loser 下一次尝试成为新 winner，以及 create bind vs cleanup/delete。另固定 late-winner 时序：双方首次 miss → winner commit → 文件被移除/损坏 → loser 文件校验失败出口二次 lookup；同 digest 必须重放，不同 digest（含 v1 key + 非空附件）必须冲突，均不得返回文件错误。当前附件随后损坏也不影响既有 v1/v2 重放。
- [ ] M3A-8. `go test ./internal/order ./internal/files ./internal/store ./internal/httpapi ./internal/app -count=1` 与对应非缓存 race 门禁通过。

## 5A-I. 首批实现门禁

- [ ] 5A-I-1. P0、M1A、M2、M3A 全部完成；同一 5A feature 候选 revision 的目标包 test/race、全仓 test/vet、文档 validator 和 diff check 通过。v6 gate 与 schema-only 产物按各自不可变 revision/hash 关联，不要求与 5A feature 同 revision。
- [ ] 5A-I-2. 真实 app smoke 覆盖上传、未绑定删除、create 绑定、普通 PATCH 修改订单字段但保留附件映射/顺序、detail/download/restart、四角色鉴权下载和 v2 幂等重放；显式 PATCH `attachmentIds` 返回严格 400，5B group/edit/orphan/order-delete 能力 fail-closed。
- [ ] 5A-I-3. STAGING commit、temp→final、UPLOADED commit、单项 delete/expiry、下载完整性校验和 create bind 的真实子进程退出矩阵收敛；无 probe 流量制造超过 5 分钟 unresolved 后，最多 5 秒内 `/readyz` not-ready、shutdown callback 只触发一次、在途请求宽限 15 秒、进程退出 75，监督器重启后由启动恢复收敛。三类构造期 schema gate 和本机 migrate/启动锁证据已记录。
- [ ] 5A-I-4. 最小 cleanup 能处理过期 UPLOADED、单项 COMMITTED 和旧 temp；发布说明明确 PATCH/5B 未启用范围。通过 5A-I 后可进入 M1B/M3B/M4 开发，但未通过 5A-D 前不得持续部署。

## 5A-D. 首批部署批准门禁

- [ ] 5A-D-1. 三产物发布矩阵完整记录 revision、干净构建命令、文件名、Go 版本、SHA-256、机器可验证 capability manifest、支持 schema、允许数据扫描、退出码、部署顺序、数据库路径、备份标识和回退演练；部署 validator 与错误 binary/schema/data 负向 smoke 通过，功能写入后不得回退到 schema-only binary。
- [ ] 5A-D-2. 选定的支持部署 profile 在真实环境验证监督器收到退出码 75 后重启；`cleanup-attachments` 调度每 15 分钟、超时 10 分钟、禁止重叠、失败告警，均以唯一 run ID 记录。ENOSPC 无残留证据已记录，且说明其不等价于容量保护；其他仅完成开发/CI 兼容的平台不得写入部署批准范围。5A 产物、CI 产物、manifest、README 和部署文档统一标注 `internal/demo-only`。
- [ ] 5A-D-3. Windows 本地 `os.Root`/reparse/打开句柄 rename/share mode 与 applock 证据、Linux CI test/race、已知限制和剩余 5B 范围已记录；发布说明明确 5A 只允许单进程、受控 demo/内部环境，不构成生产或多副本批准。缺任一项时只能短期本机/隔离测试，不得部署到持续可访问环境。

## M1B. Group journal、REMOVING 与完整恢复

- [ ] M1B-1. 实现 group BUILDING/PREPARED/COMMITTED、EDIT_REMOVE/ORDER_DELETE、明细 operation token、批量 BOUND→REMOVING、整组 restore/finalize/purge；BUILDING 只存在于组装事务，失败整体回滚。
- [ ] M1B-2. 组恢复覆盖部分 rename、部分 restore、每项 final/trash 真值组合、重复恢复和 token/phase 变化；任一明细无法安全收敛时整组 fail-closed，不得部分提交订单映射或删除 metadata。
- [ ] M1B-3. 实现 ORPHAN_FINAL claim 原语：满 24 小时后事务内重查 attachment/mapping/活动 operation，插入唯一 claim，STAGING 反向拒绝 claim，隔离前重验 token；无法证明无引用时绝不删除。
- [ ] M1B-4. 5B binary 启动扫描识别并恢复全部 group/kind；两个独立进程覆盖 edit-remove/order-delete/orphan claim 与原 owner 竞争，loser 只得到稳定 state/not-found/unavailable 分类。
- [ ] M1B-5. 在 group ownership、每个 final→trash、最终 COMMITTED、部分 restore 和 purge 边界真实进程退出，重启后订单映射、附件状态、group/op 和文件事实整组收敛。
- [ ] M1B-6. `go test ./internal/files ./internal/store ./internal/admin ./internal/order ./internal/app -count=1` 与对应非缓存 race 门禁通过。

## M3B. Edit 组移除与订单删除原语

- [ ] M3B-1. PATCH edit 区分字段缺失、显式空数组和最终集合；准备事务记录 group/order/expected version/count 并批量 `BOUND -> REMOVING`，全部 ownership 成功后才移动文件。最终事务先删除旧 `order_attachments` 映射，再按最终集合重建位置，不依赖临时 position，不再声称 writer fence 跨越两段事务。
- [ ] M3B-2. REMOVING 窗口内列表 count、详情和 Action 保留旧摘要，下载 503；竞争 edit/action/refund 可提交或稳定冲突，最终事务重验 version/refund/group/token。最终失败整组 restore，restore 失败升级 503 并保留 evidence。
- [ ] M3B-3. 覆盖组内部分 rename、部分 restore、final/trash 五种真值组合、重复恢复和每个进程退出点；成功后文件不可下载，purge 失败留下 COMMITTED tombstone，不把已提交 edit 错报为未提交。
- [ ] M3B-4. 两个独立连接覆盖 edit replace/remove vs cleanup/refund approve/另一个 edit；winner/loser 只能稳定归类为 version/state/validation/unavailable/not-found，重试后订单、退款、组 operation、附件状态和文件事实一致。
- [ ] M3B-5. 真实 app 完成 upload → create bind → detail/list → download → edit add/reorder/remove → action → 重启；断言 v2 重放仍返回首次响应且不依赖当前附件。
- [ ] M3B-6. 不注册订单删除 HTTP，直接调用 ORDER_DELETE 用例并记录 Evidence。准备阶段只允许 PREPARED group/op、BOUND→REMOVING 和文件隔离；store 单一最终事务必须同时重新验证订单/token、删除 `order_attachments` 与订单聚合行、REMOVING→DELETING、group/op→COMMITTED。成功断言订单最终不存在；最终事务失败或退出断言订单/映射仍完整并整组 restore。另覆盖多 BOUND 附件、任一文件缺失时整组失败且保留 evidence、purge 失败、重复执行和 RESTRICT 外键。
- [ ] M3B-7. `go test ./internal/order ./internal/files ./internal/store ./internal/httpapi ./internal/app -count=1` 与对应非缓存 race 门禁通过。

## M4. 清理、seed、reset 与 readiness

- [ ] M4-1. `cmd/admin cleanup-attachments` 取得共享 maintenance lock，使用注入 clock 与 batch=100 扩展处理过期 UPLOADED、自己新建的 operation、可幂等 COMMITTED、旧 temp 和已取得唯一 claim 的 orphan final；跳过其他 owner 的 STAGING/REMOVING/PREPARED，输出 `scanned/prepared/skipped/committed/purged/failed` 摘要。不得把 5A 最小 cleanup 的锁与所有权规则放宽。
- [ ] M4-2. cleanup 不删除 BOUND、未过期或根目录外文件；UTC 边界、批次边界、并发 bind/delete、取消和重复执行稳定。
- [ ] M4-3. development `attachment_demo=1` 仅在 API 停止并取得 `API exclusive -> maintenance exclusive` 后生成确定性合法 PDF/PNG/JPEG、一个已绑定和一个未绑定 fixture；seed 必须复用 `temp -> STAGING -> final -> UPLOADED -> order_attachments/BOUND` durable 协议，真实子进程覆盖每个 commit/rename 边界。重复 seed 校验 metadata/mapping/size/hash，production 不写 demo 文件。
- [ ] M4-4. reset 在 API 停止且取得独占 maintenance lock 后删除数据库、WAL/SHM 和附件根目录；与 cleanup/seed 并发时拒绝，reparse/symlink/越界路径拒绝，无关文件保留，随后 migrate/seed 恢复确定性基线。
- [ ] M4-5. API 在打开 DB/root、构造 service/handler 和注册路由前持有 API lock + 独占 maintenance lock，执行共用 schema gate、恢复全部遗留 operation，并按 500 行批次/30 秒预算核验稳定 UPLOADED/BOUND 的映射、basename、final、普通文件身份和记录 size；启动不全量 hash/type，恢复失败、缺失 final 或超预算时拒绝启动。`newAPI`/`Handler()`/嵌入式入口均覆盖门禁；保留并扩展 5A watchdog 的 5 秒 tick、一次性 callback、15 秒宽限和退出码 75，仍不在线接管，覆盖完整 group/kind、无 probe 流量、probe 成功/失败/恢复、取消和无路径泄露。
- [ ] M4-6. 全新数据目录真实 smoke 覆盖 migrate、seed、启动、登录、上传、绑定、下载、删除未绑定、过期清理、重启和 reset；记录命令、revision 和数据目录。
- [ ] M4-7. 验证矩阵“附件”改为 `enabled: yes`，最低证据命令和威胁测试与实际包名一致；页面仍保持 `enabled: no`。
- [ ] M4-8. `go test ./internal/admin ./internal/config ./internal/files ./internal/store ./internal/httpapi ./internal/app -count=1` 与对应非缓存 race 门禁通过。
- [ ] M4-9. 真实子进程 smoke 在每个 rename/commit 边界强制退出并重启 API/admin，验证 startup recovery、cleanup 重试和最终 DB/filesystem 收敛；seed 也必须覆盖每个 durable 边界；不得只用函数返回 error 的 mock 代替进程退出证据。

## 5B. 最终集成门禁

- [ ] 5B-1. 5A-I、5A-D、M3B/M4 全部完成；edit/remove/order-delete/cleanup/orphan 的目标包 test/race 和完整真实 crash matrix 通过，且不破坏已通过的 5A 行为。
- [ ] 5B-2. 新数据目录真实 app smoke 完成 upload → create bind → download → edit add/reorder/remove → action → cleanup → restart → reset；DB、operation journal 与文件系统最终收敛。
- [ ] 5B-3. 同一最终候选 revision 的全仓 test/vet/race、Windows 文件语义证据、Linux CI、文档 validator、artifact/hash 和回退演练全部通过，才能进入 R 完成报告。

## R. 文档与发布门禁

- [ ] R1. 实现及集成门禁通过后，把附件 endpoint、multipart、DTO、下载 headers、订单字段和错误契约迁入当前 HTTP API；目标 API 删除已实现附件草案和重复细节。
- [ ] R2. 同步 overview、architecture、domain model、roadmap、validation、scenario、README 和 CHANGELOG；不提前宣称页面、对象存储、病毒扫描或订单删除 endpoint。
- [ ] R3. `docs/audit/validate.ps1`、`docs/audit/validate.tests.ps1` 和 `git diff HEAD --check` 通过，跨文档角色、状态、大小、过期、路径和幂等口径一致。
- [ ] R4. 最终 revision 实跑 `go test ./... -count=1`、`go vet ./...` 和独立 `go test -race ./... -count=1`；不得用缓存、skip 或放宽 deadline 掩盖失败。
- [ ] R5. 远端 Linux CI 的 test/race run、revision 和结果已记录；Windows 本地 reparse/rename 行为与 Linux CI 差异有说明。
- [ ] R6. 交付 fail-closed 恢复手册：只读诊断命令、必须成套恢复的 DB/WAL/SHM/附件目录、允许人工隔离/删除的对象、禁止直接修改的状态，以及修复后重新执行完整性扫描的步骤。完成报告列出已实现范围、未执行项和剩余风险；只有用户明确确认后才移动 plan/checklist 到 `archived/` 并更新归档索引。
