---
status: active
owner: 后端团队
created: 2026-07-12
last_updated: 2026-07-12
applies_to: implementation roadmap phase 2 authentication and authorization
---

# 阶段二：认证授权 Checklist

配套计划：[阶段二认证授权开发计划](./0002-2026-07-12-plan.md)。只有实现、测试、文档与验证证据全部满足时才勾选；计划本身完成不代表阶段二实现完成。

## A. 开工基线与依赖

- [ ] A1. 记录 `go test ./... -count=1`、`go vet ./...` 和 `go test -race ./...` 的开工基线。
- [ ] A2. 阅读 bcrypt 当前官方文档，确认支持的 Go 版本、密码长度上限、cost 范围和 hash 格式；批次 A 结束前根据实测固定 cost 并回写计划与证据。
- [ ] A3. 阅读 `golang-jwt/jwt/v5` 当前官方文档，确认 HS256 key 要求、parser options、registered claims 和错误分类。
- [ ] A4. 固定两个模块的具体版本，核对 `go.mod` / `go.sum` 只有预期依赖变化。
- [ ] A5. 确认本轮非目标未进入实现：账号管理 API、refresh token、OIDC、订单、附件、看板和页面配置。

证据：

- 基线命令与结果：待记录。
- 依赖版本、官方 URL 与逐项结论：待记录。

## B. 配置与密钥边界

- [ ] B1. `JWT_SIGNING_KEY` 在 API development/production 均必须显式配置且至少 32 bytes。
- [ ] B2. 仓库、测试快照、日志和错误中不存在默认或真实签名密钥。
- [ ] B3. 配置提供 `LoadBase`、`LoadAPI`、`LoadDemoSeed`、`LoadBootstrapAdmin` 四个显式入口；admin 先解析子命令再选择 loader，不再启动时无条件调用单一 `Load`。
- [ ] B4. `cmd/api` 使用 `LoadAPI` 加载基础配置与 JWT 密钥，配置错误在监听端口和打开数据库前失败。
- [ ] B5. `cmd/admin migrate` 只使用 `LoadBase`，不读取 JWT 密钥或演示密码。
- [ ] B6. `cmd/admin seed` 先使用 `LoadBase`；development 再使用 `LoadDemoSeed` 并要求密码 12..72 bytes，production 不读取演示密码。
- [ ] B7. `cmd/admin reset` 先以基础配置拒绝 production；development 在打开数据库、获取锁或删除文件前通过 `LoadDemoSeed` 校验演示密码。
- [ ] B8. `cmd/admin bootstrap-admin` 仅在 production 使用 `LoadBootstrapAdmin`，要求显式 username 与 password，所有输入校验早于数据库打开。
- [ ] B9. 配置测试覆盖缺失、空值、31/32-byte key、密码 11/12/72/73-byte 边界、按命令读取 secret 和所有校验早于数据库打开。
- [ ] B10. `go test ./internal/config ./internal/admin -count=1` 通过。

证据：待记录。

## C. Migration v2 与认证 Seed

- [ ] C1. 新增连续的 `0002_auth.sql`，只创建 `users`、`sessions`、约束和必要索引。
- [ ] C2. `users.username` 保存规范化值且唯一，role 数据库约束只允许四个内置角色。
- [ ] C3. `sessions.user_id` 具有外键，`token_id` 唯一，过期与撤销时间可查询。
- [ ] C4. migration v2 不写演示账号、密码 hash 或环境相关数据。
- [ ] C5. 仅应用 v1 的真实数据库被 readiness 分类为 `schema_outdated`。
- [ ] C6. v1 → v2 升级保留已有 `seed_versions`，重复 migrate 无副作用。
- [ ] C7. migration v2 失败时 schema 与 `user_version` 均不前进。
- [ ] C8. auth demo seed 使用独立 `auth_demo=1` group，不改变 `runtime=1`。
- [ ] C9. development `admin seed` 与 reset 后 seed 固定按 `runtime` → `auth_demo` 顺序执行。
- [ ] C10. 任一 seed group 失败使整条命令失败且不报告整体成功；每个 group 保持自身事务原子性，已提交前序 group 的状态被准确报告。
- [ ] C11. development seed 创建 username/role 同名的 `viewer`、`operator`、`approver`、`admin` 四个账号，密码只存 bcrypt hash。
- [ ] C12. `auth_demo` version 等于 `1` 时仍执行只读一致性校验，确认四个账号存在、角色正确、未禁用且 hash 匹配当前演示密码，不直接跳过。
- [ ] C13. 测试先成功 seed，再分别篡改/删除账号、修改角色、禁用账号或更换 hash，重复 seed 必须失败且不修改账号或 seed version。
- [ ] C14. 重复 auth seed 不重复账号；固定身份发生冲突时安全失败且 auth group 事务回滚。
- [ ] C15. production seed 只运行 `runtime`，不读取演示密码、不创建账号且不写 `auth_demo` seed version。
- [ ] C16. 所有 migration/seed 测试使用 `t.TempDir()` 和测试密码，不访问开发数据库。
- [ ] C17. production migrate 后，`bootstrap-admin` 在 users 空表的单一事务内创建一个 role=`admin` 的规范化账号，输出不含密码或 hash。
- [ ] C18. development、未迁移库、users 非空、缺失/非法输入和成功后的重复调用均安全失败；两个独立连接并发 bootstrap 时恰好一个成功、一个被拒绝，最终只有一个 admin。
- [ ] C19. `bootstrap-admin` 对非空 users 表不更新账号、不重置密码，也不提供第二账号或角色管理能力。
- [ ] C20. `go test ./internal/store ./internal/admin -count=1` 通过。

证据：待记录。

## D. 密码、角色与 JWT 核心

- [ ] D1. 密码 hash/compare 封装在 `internal/auth`，HTTP 和 store 不直接调用 bcrypt。
- [ ] D2. bcrypt cost 固定并有测试；所有密码长度使用 UTF-8 编码后的 byte 数校验为 12..72，支持识别低 cost hash 供未来升级且登录时不静默改库。
- [ ] D3. 未知用户名使用进程启动时预生成、与真实 hash 相同 bcrypt cost 的 dummy hash，不按请求重复生成；对外结果与错误密码一致，已知与未知用户在 compare 前使用相同的密码长度校验路径。
- [ ] D4. username 固定使用 `strings.TrimSpace` + `strings.ToLower`，数据库查询与 limiter key 复用同一结果；密码不 trim、不做 Unicode 变换。
- [ ] D5. `Role` 只允许 viewer/operator/approver/admin，授权使用显式 allowlist/capability。
- [ ] D6. token 使用 HS256 和至少 32-byte key，TTL 固定 15 分钟。
- [ ] D7. claims 包含并验证 `sub`、role、`exp`、`iat`、`jti`、issuer 和 audience；自定义 validation 强制 `sub`、`iat`、`jti` 和 role 非空且 role 合法。
- [ ] D8. parser 显式使用 `WithValidMethods`、`WithExpirationRequired`、`WithIssuer`、`WithAudience`、`WithIssuedAt`、`WithTimeFunc` 和 30 秒 `WithLeeway`。
- [ ] D9. 测试覆盖篡改签名、`alg=none`、错误 HMAC 算法、issuer/audience、claims 缺失和格式错误。
- [ ] D10. 注入 clock 覆盖 30 秒临界 leeway：拒绝 `iat > now + 30s`，并在 `now >= exp + 30s` 时判定 JWT 过期；测试覆盖等号前一瞬与等号点且不使用 sleep。
- [ ] D11. 测试分别删除 `sub`、`iat`、`jti` 或 role，证明 `WithIssuedAt` 的可选语义不会允许缺失 `iat`，且 parser 使用注入 clock 而非进程时间。
- [ ] D12. ID 与 `jti` 使用注入的 `crypto/rand` 默认实现，生成失败向上传播且不创建 session。
- [ ] D13. `go test ./internal/auth -count=1` 通过。

证据：待记录。

## E. 用户、Session 与认证服务

- [ ] E1. auth service 通过窄 store interface 查询用户、创建 session、认证 principal 和撤销 session。
- [ ] E2. 登录成功创建唯一 session，JWT 的 `sub`/role/`jti` 与数据库一致，session `expires_at` 与 JWT `exp` 完全相同。
- [ ] E3. 同一用户可并存多条未撤销 session，新登录不撤销既有 session。
- [ ] E4. 用户不存在、密码错误或用户禁用均不创建 session。
- [ ] E5. 每次认证同时检查 JWT、session 存在、未撤销、未到期、`session.user_id == claims.sub` 和用户未禁用。
- [ ] E6. 每次认证读取当前用户角色；数据库角色与 token role 不一致时拒绝旧 token。
- [ ] E7. session 到期以服务端 clock 与数据库值为准，在 `now >= expires_at + 30s` 时过期，与 JWT `exp` 边界完全一致。
- [ ] E8. logout 使用条件更新只撤销当前 session，不撤销同一用户的其他 session。
- [ ] E9. 两个在撤销前均通过 middleware 的并发 logout 可幂等返回 `204`；撤销提交后新到达的旧 token 请求返回 `401`，第二次条件更新未命中不得变成 `500`。
- [ ] E10. store 关闭、context 取消、事务失败和随机源失败均返回类型化内部错误且不泄露细节。
- [ ] E11. repository/service 测试覆盖重复 `jti`、多 session、session 缺失、subject/session 错配、撤销、到期、禁用和角色变化；用户 A 的合法 `jti` 搭配用户 B 的 `sub` 必须被拒绝。
- [ ] E12. `go test ./internal/auth ./internal/store -count=1` 通过。
- [ ] E13. 已记录本阶段不清理过期/撤销 session：development 依赖 reset，production 会持续增长且不得宣称生产完备；索引为后续 purge 保留基础。

证据：待记录。

## F. 登录限流与输入边界

- [ ] F1. login 只接受 JSON，body 上限固定为 4 KiB，并拒绝未知字段和多余 JSON 值。
- [ ] F2. username/password 缺失、类型错误、空值和超长输入返回安全 `INVALID_REQUEST`；覆盖 ASCII 与多字节 Unicode 的 11/12/72/73-byte 边界，不按 rune 数判断。
- [ ] F3. limiter 从 `RemoteAddr` 用 `net.SplitHostPort` + `net/netip` 提取规范 IP；同 IP 不同端口得到同一 key，覆盖 IPv4、IPv6、malformed address 与固定 `unknown` 分区，且不信任代理 header。
- [ ] F4. 每个 key 在 1 分钟内最多 5 次失败；第 6 次及后续在密码校验前返回 `429 RATE_LIMITED`，正确密码也不能绕过。
- [ ] F5. 成功登录清除当前 key 的失败计数，不影响其他用户名或 IP。
- [ ] F6. limiter clock 可注入，窗口边界测试不使用 sleep。
- [ ] F7. `Retry-After` 为剩余窗口秒数向上取整且最小为 `1`，窗口结束后恢复认证。
- [ ] F8. 只有未知用户、错误密码和禁用账号增加计数；`400`、`415`、context 取消/超时和内部错误不计数，`429` 不增加计数或延长窗口。
- [ ] F9. limiter 最多保存 4096 个活跃 bucket且每个规范 IP 最多占用 32 个 username bucket；`unknown` 是独立 IP 分区。
- [ ] F10. 单 IP 达到 32 个 key 后，新 username 返回 `429` 与 `Retry-After: 1`，不插入 bucket且不影响其他 IP。
- [ ] F11. 全局满载时只淘汰最久未访问且失败数小于 5 的未封禁 bucket；窗口内失败数达到 5 的受限 bucket不可淘汰，全部受保护时新 key fail-closed。
- [ ] F12. 单 IP 大量用户名与多 IP 高基数测试证明 bucket 总数不超过 4096，单 IP 不能耗尽全局容量或使其他 IP 的已有/新登录被全局拒绝。
- [ ] F13. 过期 bucket 被清理，窗口结束后可为新 key 释放容量。
- [ ] F14. 并发失败请求、每 IP 配额与并发满载计数正确且通过 race detector。
- [ ] F15. `go test ./internal/httpapi ./internal/auth -count=1` 通过。

证据：待记录。

## G. HTTP 认证接口

- [ ] G1. `POST /api/v1/auth/login` 成功返回 `200`、`Content-Type: application/json`、Bearer token、expiry 和用户摘要。
- [ ] G2. 未知用户名、错误密码和禁用账号返回相同 `401 AUTHENTICATION_FAILED` envelope。
- [ ] G3. `GET /api/v1/auth/me` 返回 `200`、`Content-Type: application/json`、当前用户摘要和 token expiry。
- [ ] G4. `POST /api/v1/auth/logout` 成功返回 `204`、空 body并撤销当前 session；两个已认证的并发 logout 均可返回 `204`。
- [ ] G5. auth middleware 只接受单个、格式正确的 `Authorization: Bearer <token>`；auth-scheme 大小写不敏感，token 值保持原样且区分大小写。
- [ ] G6. 缺失/重复 header、错误 scheme、空 token、篡改/过期 token 和失效 session 返回 `401 UNAUTHENTICATED`。
- [ ] G7. 所有 `401` 返回 `WWW-Authenticate: Bearer`，错误 body 复用阶段一 request ID envelope。
- [ ] G8. 三个 path 的错误 method 返回 `405`、准确 `Allow` 和 `METHOD_NOT_ALLOWED`。
- [ ] G9. 错误 Content-Type 返回 `415`；body 格式、字段和大小错误返回 `400`，不进入密码 compare。
- [ ] G10. request context 取消、认证 store 关闭和内部错误安全返回，不 panic、不泄露 SQL/路径。
- [ ] G11. principal context helper 对缺失值安全失败，业务 handler 不解析 JWT。
- [ ] G12. 仅测试代码中的真实 HTTP 角色策略 handler 覆盖四个角色的允许/拒绝矩阵，并为后续 endpoint 提供统一 `403 FORBIDDEN`；这是阶段二角色越权的 transport/policy 集成证据，生产 mux 不注册虚构业务路由。
- [ ] G13. 中间件链验证为 request ID → access log → recovery → auth → protected handler；login、health、readiness 不经过 auth，panic 与认证失败仍被 recovery/access log 正确观察。
- [ ] G14. `go test ./internal/httpapi ./internal/auth -count=1` 通过。

证据：待记录。

## H. 敏感信息与日志

- [ ] H1. login 请求密码、Authorization header 和完整 JWT 不进入 access/error 日志。
- [ ] H2. 密码 hash、签名密钥和 session token ID 不进入响应、日志或 wrapped error 文本。
- [ ] H3. 认证日志只记录 request ID、结果类别和必要的非敏感诊断字段。
- [ ] H4. 未知用户与已知用户失败日志不提供可枚举差异。
- [ ] H5. 测试使用捕获 logger 扫描 password、hash、token、key 和 Authorization 原文。
- [ ] H6. panic recovery 与认证内部错误仍保持安全 envelope。

证据：待记录。

## I. 应用集成与回归

- [ ] I1. app 集成测试使用临时 SQLite、测试密钥、注入 clock 和真实 HTTP，不共享全局状态。
- [ ] I2. 集成流程覆盖 migrate → runtime/auth seed → login → me → logout → 旧 token 被拒绝。
- [ ] I3. 四个 seed 账号分别登录并得到正确角色。
- [ ] I4. 全新 production 临时数据库完成 migrate → bootstrap-admin → API 启动 → login，第二次 bootstrap 被拒绝。
- [ ] I5. app 集成覆盖错误密码、未知/禁用账号、过期/篡改 token、subject/session 错配和撤销 session；真实业务路由角色越权延后到阶段三。
- [ ] I6. 进程重开且使用同一签名密钥后，未过期且未撤销 session 仍可认证。
- [ ] I7. 不同签名密钥不能验证旧 token，且不修改原 session。
- [ ] I8. `/healthz` 与 `/readyz` 现有契约不回归；auth 失败不影响 liveness。
- [ ] I9. `go test ./... -count=1` 通过。
- [ ] I10. `go vet ./...` 通过。
- [ ] I11. `go test -race ./...` 通过；不能执行时记录具体原因且不得勾选。
- [ ] I12. app 装配测试验证 API 配置 → store → password/JWT service → auth service → limiter → handler 的完整依赖注入链路及关闭顺序。

证据：待记录。

## J. 文档与门禁收敛

- [ ] J1. 当前 HTTP API 新增三个已实现 auth endpoint、字段、header、状态码和测试证据。
- [ ] J2. 目标 HTTP API 删除认证 draft，只保留未实现能力。
- [ ] J3. 验证矩阵的认证改为 `enabled: yes`，记录实际测试入口。
- [ ] J4. 配置与 README 记录 `JWT_SIGNING_KEY`、四个固定 username、`DEMO_ACCOUNT_PASSWORD` 设置方式、production `bootstrap-admin` 流程和限制，运行时与文档均不写具体密码值。
- [ ] J5. 领域模型将“提示演示密码”收敛为提示环境变量配置方式，并明确 development 使用四角色 seed、production 使用空库限定 bootstrap、订单 seed 仍属阶段三。
- [ ] J6. 架构和路线图同步实际 users/session/role 边界、多 session 策略与阶段二状态。
- [ ] J7. CHANGELOG 记录已实现能力，不把账号管理或后续业务写成已交付。
- [ ] J8. `git diff --check`、Markdown 相对链接检查和 frontmatter 检查通过。
- [ ] J9. README 或架构文档集中记录已知限制：无 refresh token、无 session 清理、单实例内存限流不持久化、无可信代理/CORS、bootstrap 仅限空库，以及 username 无 NFKC/同形字符归一化。

证据：待记录。

## K. 完成与归档

- [ ] K1. 计划完成标准全部满足，非目标未被意外纳入。
- [ ] K2. 向用户汇报实现、验证、未执行项和剩余风险。
- [ ] K3. 获得用户明确归档确认。
- [ ] K4. plan/checklist 全部完成后移入 `archived/`，更新活跃与归档索引。

## 最终验证记录

| 日期 | 命令/检查 | 结果 | 备注 |
|---|---|---|---|
| 待记录 | `go test ./... -count=1` | 待执行 | |
| 待记录 | `go vet ./...` | 待执行 | |
| 待记录 | `go test -race ./...` | 待执行 | |
| 待记录 | `git diff --check` | 待执行 | |