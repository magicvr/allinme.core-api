---
status: active
owner: 后端团队
created: 2026-07-12
last_updated: 2026-07-12
applies_to: implementation roadmap phase 3 order query and fulfillment
---

# 阶段三：订单查询与履约 Checklist

配套计划：[阶段三订单查询与履约开发计划](./0003-2026-07-12-plan.md)。plan §2-§4 是领域与 HTTP 冻结值的单一事实源；checklist 只描述验证结果、命令和批次差异，若摘要措辞冲突，以 plan 为准并同步修正文档。只有代码、测试、文档和验证证据齐全后才勾选；不得以最终全量测试替代批次内聚焦验证。证据标签约定为 `[test]`、`[review]`、`[doc]`、`[manual-smoke]`；复合条目必须分别记录各子断言的证据，不以一个总命令替代全部子断言。

## A. 开工基线与边界

- [x] A1. 记录 `go test ./... -count=1`、`go vet ./...` 和 `go test -race ./...` 的开工基线。
- [ ] A2. 阶段三负责人和批次 A reviewer 在首个代码变更进入评审前确认：范围只包含订单查询、草稿写入、履约 Action、订单 seed 和所需幂等能力；退款、附件、看板、页面 YAML、账号管理、库存、支付网关和物流集成均未进入 diff，并在本节证据栏记录结论。[review]
- [ ] A3. 通过 `internal/order` 包内表驱动测试或导出常量断言固化 plan §2-§3 的状态、角色、`CNY`、金额上限、随机 ID、version 初值与冲突分类，不在实现时另选常量；不得只写“代码审查通过”。[test]
- [ ] A4. 批次 E1 transport 负责人和 reviewer 在 E1 合并前，通过 `internal/order`/`internal/httpapi` 包内契约测试固化 plan §3 的 DTO、成功状态、query、分页、排序、envelope、body 上限、400/422 分工、capability、附件缺失语义和创建专用幂等契约，并记录测试名称与实际命令；不得只写“代码审查通过”，也不阻塞批次 A-D 的领域/repository 交付。[test]

证据：2026-07-12T15:59:04+08:00，Go `go1.26.0 windows/amd64`，Git revision `fc65c21f0867c073898e73fb7d5ef003b0c2a0b8`；`go test ./... -count=1: PASS`、`go vet ./...: PASS`、`go test -race ./...: PASS`、`git diff HEAD --check: PASS`，无既有失败。订单代码尚不存在；工作区已有阶段三规划文档修改，revision 仅标识代码基线，后续回归以命令结果与阶段三 diff 共同判断。

## B. Migration v3 与数据库约束

- [ ] B1. 新增连续的 migration v3，只包含订单、订单项、幂等记录及必要索引。
- [ ] B2. `orders.customer_name` 使用 `length(CAST(customer_name AS BLOB)) BETWEEN 1 AND 120`；orders 另分别 CHECK 枚举、`CNY`、金额、正 version 和 RFC3339 `Z` 文本形状，repository 测试负责真实解析与 UTC 规范化。
- [ ] B3. `order_items` 保存唯一 position、使用 `ON DELETE CASCADE`；sku/name 的 UTF-8 byte CHECK 分别使用 BLOB length `1..64`/`1..160`，数量/单价范围与 service 一致。
- [ ] B4. 幂等表按 principal user ID + method + route/operation + key 唯一，保存冻结的 method/route/digest/order ID、`snapshot_version=1` 和 snapshot v1 JSON；snapshot 不含 capability，未知版本/损坏 JSON/元数据错配安全失败且不查询当前订单或重新创建。
- [ ] B5. 状态、支付状态、创建时间和 allowlist 排序具备结构化查询所需索引；contains 关键词搜索允许参数化扫描，不引入 FTS5、不声称普通 B-tree 优化前导 `%`。
- [ ] B6. migration 不写环境相关订单数据，业务 seed 使用独立版本 group。
- [ ] B7. v1→v2→v3 升级保留用户、session 和 seed version；重复 migrate 无副作用。
- [ ] B8. 提供 package-private `applyMigration(ctx, tx, migration)` 或等价可测试迁移 seam；生产 `Migrate` 继续使用 go:embed migration，内部测试直接传入先建部分 v3 对象后故意失败的 version=3 migration，不修改 embedded filesystem；证明仍为 v2、保留 v1/v2 且无任何 v3 表/索引/trigger，随后同一 DB 正常迁移成功。[test]
- [ ] B9. readiness 对仅到 v2 的数据库分类为 `schema_outdated`，升级后恢复 ready。
- [ ] B10. `go test ./internal/store -count=1` 通过。

证据：待记录。

## C. 领域原语与能力计算

- [ ] C1. `internal/order` 定义合法订单状态、支付状态和领域错误，不依赖 HTTP 或 SQL。
- [ ] C2. 金额只使用整数最小货币单位，不使用 float；总额完全由明细计算。
- [ ] C3. 金额计算拒绝空明细、`quantity` 超出 `1..10_000`、`unitPrice` 超出 `1..9_999_999_999`、乘加溢出或总额超过 `9_999_999_999`。
- [ ] C4. 创建/编辑只允许 `CNY`，阶段三不接受其他 ISO 4217 币种。
- [ ] C5. 状态机允许 `DRAFT→CONFIRMED→FULFILLING→SHIPPED→COMPLETED`。
- [ ] C6. 取消只允许 `DRAFT`、`CONFIRMED`、`FULFILLING→CANCELLED`。
- [ ] C7. 所有其他转换保持数据不变并返回业务状态冲突。
- [ ] C8. 查看允许四角色，写入/推进只允许 operator/admin。
- [ ] C9. 订单 service 复用 `auth.Principal`、`auth.Role` 和 `auth.RoleAllowed`，不复制角色常量或通过字符串排序授权；领域模型和目标 API 同步 admin 超集语义。
- [ ] C10. operator/admin 的 `canAdvance` 仅在 DRAFT/CONFIRMED/FULFILLING/SHIPPED 存在唯一下一步时为 true，终态及 viewer/approver 为 false；`canCancel` 独立计算，退款 capability 恒为 false。
- [ ] C11. clock 与 ID 生成器可注入；生产 ID 为 `ord_`/`itm_` + 32 位小写 hex 的 128-bit 随机值，测试不使用 sleep 或时间推导 ID。
- [ ] C12. `go test ./internal/order -count=1` 通过。

证据：待记录。

## D. Repository 与查询

- [ ] D1. repository 使用参数化 SQL 和固定排序映射，不拼接客户端字段。
- [ ] D2. 列表实现全部冻结 query；每种主排序都追加 `id` 同方向二级排序，并以跨页测试证明并列值顺序稳定、无重复或遗漏。
- [ ] D3. `q` trim 后最多 200 UTF-8 bytes，对订单 ID/客户名/SKU/明细名做 ASCII 大小写不敏感匹配，中文原值比较，特殊 LIKE 字符按字面量处理。
- [ ] D4. `TestListOrdersUsesSingleSnapshot` 或等价测试在同一显式 ReadTx 内执行 COUNT 与 page；测试协调点期间不释放读事务，第二个独立 DB 在 page 前提交写入，并断言 total 与具体 items 均保持旧 snapshot；使用不超过 10 秒的 deadline，cleanup 释放协调点、回滚事务并关闭两个 DB。
- [ ] D5. 列表证明固定 COUNT + page 的有界查询计划，通常为两条 SQL；允许等价 executor/query 组织和参数化 EXISTS/受控 join 访问 order_items，但禁止逐订单 item 查询、逐行事务和第三条补查询；capability 只由 principal 与 status 计算。[test]
- [ ] D6. 恶意 filter/sort/query 不改变 SQL 结构且返回安全输入错误。
- [ ] D7. 详情在同一一致性读取中返回按 `position ASC` 排列的明细；非法格式或不存在的 orderId 均为 `NOT_FOUND`。
- [ ] D8. `TestListOrdersUsesBoundedQueries` 或等价测试通过测试 executor 证明列表在一个只读事务内执行 bounded COUNT + page，无逐行查询或逐行事务；测试名称和 executor 实现可替换。[test]
- [ ] D9. `TestOrderRepositoryRejectsCorruptRows`、关闭数据库和 context 用例覆盖扫描失败/损坏数据/关闭/取消的 typed error 分类。
- [ ] D10. `go test ./internal/store -run 'TestListOrders|TestOrderRepository' -count=1` 与 `go test ./internal/store -count=1` 通过。

证据：待记录。

## E. 创建、编辑与幂等

- [ ] E1. create DTO 使用 camelCase，固定接受 `customerName`、必填 `currency=CNY`、`items[{sku,name,quantity,unitPrice}]`；不接受客户端 ID、总额、状态或 version 字段。
- [ ] E2. `customerName`/`sku`/item `name` trim 后分别为 `1..120`/`1..64`/`1..160` UTF-8 bytes，保存 trim 后值，items 数量为 `1..100` 且保留数组顺序。
- [ ] E3. 创建在单一事务内写入订单、带 position 的明细和幂等结果快照，初始状态为 `DRAFT`、支付状态为 `UNPAID`、version 为 `1`。
- [ ] E4. edit DTO 在 create 三个事实字段外只增加 version，不接受客户端 ID、总额或状态字段；编辑只允许 `DRAFT`，提交当前 version，并在同一事务重写明细、position 和总额。
- [ ] E5. `createdAt` 永不改变；成功 edit/Action 只递增一次 version 并用注入 clock 更新 `updatedAt`，失败与幂等重放不更新时间。
- [ ] E6. `Idempotency-Key` 按固定规则校验；相同主体/method/route/key/规范请求重放首次创建的 snapshot v1，即使当前订单已编辑/推进也仍返回创建时 version 1、原始时间与明细。
- [ ] E7. 相同主体/method/route/key 的不同 normalized digest 返回 `IDEMPOTENCY_CONFLICT`；不同 method 或 route/operation 使用独立幂等作用域，可复用同一 key。记录内 method/route 与查询 scope 错配只作为损坏记录返回 `INTERNAL_ERROR`。
- [ ] E8. 不同主体可使用相同 key，结果互不泄露或冲突。
- [ ] E9. 幂等摘要基于 trim/校验后的 normalized typed command canonical JSON；`Alice` 与边缘空白变体摘要相同，明细顺序不同则摘要不同。
- [ ] E10. `PATCH` 和五个 Action 明确忽略 `Idempotency-Key`，不校验、不报 400、不保存记录，只使用 version。
- [ ] E11. 两个独立连接竞争同一 key 时最多创建一个订单，不产生第二个订单或第二份 snapshot；使用不超过 10 秒的 deadline，并在 cleanup 释放 barrier、回滚事务、关闭 DB 和删除临时目录。[test]
- [ ] E12. snapshot v1 按冻结类型包含 order 与有序 items，不含 capability；读取时核对 principal/method/route/key scope、digest/order ID/version 及 DRAFT/UNPAID/version=1/createdAt=updatedAt，未知版本、损坏 JSON、元数据错配均安全失败。
- [ ] E13. 重放只从 snapshot v1 恢复订单事实并按当前已认证 operator/admin 重算 capability，不查询当前订单补字段；后续订单变化不污染创建快照。
- [ ] E14. ReplayCreate 使用窄的幂等读取接口；`TestReplayCreateDoesNotReadCurrentOrder` 或等价测试注入调用即失败的 GetOrder/current-order stub，证明 replay 只读幂等记录/snapshot、不访问 orders/order_items；`go test ./internal/order ./internal/store -run 'Create|Idempot' -count=1` 通过。

证据：待记录。

## F. 履约 Action 与乐观锁

- [ ] F1. confirm、fulfill、ship、complete、cancel 共用明确的 service/state transition 边界，不在 handler 复制规则。
- [ ] F2. 每个 Action 在事务内读取当前资源，校验后使用 ID + 源状态 + version 条件更新；未命中后重新读取并分类。
- [ ] F3. 每个合法转换成功后状态正确且 version 只递增一次。
- [ ] F4. 按存在性→version→状态分类：不存在返回 `NOT_FOUND`，旧 version 返回 `VERSION_CONFLICT`，当前 version 下非法源状态返回 `STATE_CONFLICT`；version 与状态同时不匹配时返回 `VERSION_CONFLICT`。
- [ ] F5. viewer/approver 调用所有写 Action 返回 `403` 且数据库不变化。
- [ ] F6. operator/admin 的允许矩阵一致；不存在基于角色字符串排序的隐式授权。
- [ ] F7. 对同一 version 的并发 Action 最多一个成功，其他请求返回版本冲突且无重复状态变化。
- [ ] F8. 已完成/已取消终态不能继续推进或取消。
- [ ] F9. context 取消、数据库关闭和事务失败不产生部分更新。
- [ ] F10. `go test ./internal/order ./internal/store -count=1` 通过。

证据：待记录。

## G. 订单 Seed 与 Reset

- [ ] G1. development 订单 seed 使用独立 group/version，并按 runtime→auth_demo→order_demo 顺序执行。
- [ ] G2. admin 的 `seedDemo`/`resetDemo` 通过结果或 seed_versions 顺序断言 runtime→auth_demo→order_demo；production path 为 order seed 传当前 UTC，测试直接传固定 appliedAt。
- [ ] G3. seed 使用六个固定 ID 覆盖 plan §6 组合且不创建 DRAFT/CANCELLED + PAID；另测数据库可读取枚举各自合法的其他组合，证明该矩阵不是交叉 schema CHECK。
- [ ] G4. 固定 ID `...0003`、`...0004`、`...0005` 分别作为 FULFILLING/SHIPPED/COMPLETED 的阶段四退款前置订单。
- [ ] G5. seed 金额与明细计算一致，固定时间/ID 支持确定性查询断言。
- [ ] G6. 六个订单时间固定为 `2026-01-01T00:00:00Z` 起逐时递增且 updatedAt=createdAt；`SeedOrderDemo(ctx, appliedAt)` 仅将注入时间用于首次 seed_versions 元数据，重复校验不更新时间。
- [ ] G7. 重复 seed 不重复订单，并对缺失、篡改状态、金额、明细或支付状态执行只读一致性校验。
- [ ] G8. seed 不预创建退款、附件或看板快照；后续阶段的数据由各自 seed group 管理。
- [ ] G9. production seed 不创建演示订单或写入 order_demo version。
- [ ] G10. development reset 后恢复相同订单基线，production reset 继续被拒绝。
- [ ] G11. `go test ./internal/store ./internal/admin -run 'Seed|Reset' -count=1` 通过。

证据：待记录。

## H. HTTP 查询与详情

- [ ] H1. `GET /api/v1/orders` 和 `GET /api/v1/orders/{orderId}` 经过统一 auth middleware。
- [ ] H2. 四角色均可查看列表与详情，未认证请求返回 `401`。
- [ ] H3. 列表项严格返回 plan §3 的 Order DTO 全部标量与 capability 字段，不含 `items` 或 `position`。
- [ ] H4. 详情返回 Order DTO 的全部五个 `canXxx` 和按 position 排序的 items；item 不含 position，响应不出现附件字段或附件 capability。
- [ ] H5. query 缺失、格式错误、重复参数、非法枚举、越界分页和非法 sort 统一返回 `400 INVALID_REQUEST`，并使用冻结的 `error.details` 字段名。
- [ ] H6. method 不匹配返回 `405` 与准确 `Allow`；订单 GET endpoint 对 HEAD 显式返回 `405 METHOD_NOT_ALLOWED`，不得被 `http.ServeMux` 的 GET/HEAD 匹配行为放行；非法格式和不存在的 orderId 均返回 `404 NOT_FOUND`。
- [ ] H7. context 取消、超时、store 关闭和内部错误不 panic、不泄露 SQL/路径。
- [ ] H8. `go test ./internal/httpapi ./internal/order -count=1` 通过。

证据：待记录。

## I. HTTP 写入与 Action

- [ ] I1. `config.LoadAPI` 校验并装配 `config.APIConfig.CORSAllowedOrigin`，覆盖 development/production 的未设置、合法 http/https、通配符、多个 origin、userinfo、path/query/fragment；非法配置在 app 构造前失败，不监听端口、不创建/打开数据库、不执行 migration。
- [ ] I2. middleware 顺序为 request ID→access log→recovery→CORS→authn→authz→handler；CORS 拒绝有 request ID、进入日志，无 Origin 完全绕过，非法 actual Origin 在 auth 前拒绝。
- [ ] I3. 共享 route metadata 同时驱动 known path、mux 注册、supported method、OPTIONS allowlist 和 `Allow`；测试逐 route 对照三者，防止只注册其中一份；known path+OPTIONS 才进入 CORS preflight，unknown path 继续 mux 返回 404。
- [ ] I4. 已知 path 的合法预检返回 `204` 与冻结 header，并返回 `Vary: Origin, Access-Control-Request-Method, Access-Control-Request-Headers`；拒绝预检也断言相同缓存区分头且返回 `403`；实现追加/合并 `Vary` 而不覆盖其他 middleware 值；`OPTIONS /unknown-path` 返回普通 `404` 且无 allow-origin。
- [ ] I5. create/edit/Action 要求 JSON Content-Type，分别使用 `64 KiB`/`1 KiB` 上限并拒绝未知字段和多余 JSON 值。
- [ ] I6. quantity/unitPrice/version 使用 `json.Number` 且只接受普通十进制整数；`1.0`、`1e2`、字符串、null、超 int64 返回 `400`，范围错误返回 `422`。
- [ ] I7. 创建要求合法 Idempotency-Key；缺失或非法返回 `400` 和规范 header detail。
- [ ] I8. PATCH 成功返回当前订单且只更新 updatedAt；Action body 仅含 version，成功返回当前订单。
- [ ] I9. create 首次/重放返回 snapshot v1 语义；viewer/approver 写请求返回 `403`。
- [ ] I10. 版本、状态、幂等、字段和不存在错误稳定可区分。
- [ ] I11. 相同幂等请求首次/重放均返回 `201` 和同一快照语义。
- [ ] I12. 所有冻结状态使用统一 request ID envelope；`TestOrderHTTPMapsUnavailable` 或等价测试证明 busy/locked 的 typed Unavailable 映射 `503` + `Retry-After: 1`，同 key 重试返回首次 snapshot，不按错误字符串判断。[test]
- [ ] I13. error.details schema、字段命名、顺序与省略规则稳定。
- [ ] I14. version 结构/语义/冲突分别映射 `400/422/409`。
- [ ] I15. 组合输入按 route/method→CORS→authn→authz→Content-Type→header/query/path→JSON→typed validation→resource→version→state 短路，覆盖 unknown path、非法 Origin+缺 token、允许 Origin 下 viewer+坏 Content-Type、operator+坏 Content-Type/JSON、非法 orderId+坏 JSON。
- [ ] I16. handler 未提交响应时 context cancel/timeout 通过 `http.ErrAbortHandler` 或等价连接中止机制中止连接，不写业务 envelope、不映射 503；recovery 不把取消路径映射为 `500 INTERNAL_ERROR`；使用真实 `httptest.Server` 或连接级客户端测试，设置不超过 10 秒的 deadline，并在 cleanup 关闭 server/连接和释放协调资源。[test]
- [ ] I17. offset query 规范化为 UTC instant，响应始终为 Z，边界比较不使用原始字符串。
- [ ] I18. `go test ./internal/config ./internal/httpapi ./internal/order -run 'CORS|Order|Context' -count=1` 与包级聚焦命令通过。

证据：待记录。

## J. 应用集成与并发

- [ ] J1. 只有 `NewAuthenticatedAPI`/`NewAuthenticatedAPIWithDependencies` 打开共享 DB、装配 auth/order service 并注册订单路由；任一依赖构造失败则启动失败。`NewAPI` 不打开业务 DB、不注册认证/订单路由，订单 path 稳定返回 404；`OrderService == nil` 时 handler 不注册半成品路由且不 panic。[test]
- [ ] J2. 认证订单路由经过 auth middleware；应用关闭顺序为 server shutdown→probe→共享 DB→process lock，并有重开行为证据。[test]
- [ ] J3. 真实 HTTP 流程覆盖登录→创建→查询→编辑→确认→履约→发货→完成。
- [ ] J4. 真实 HTTP 流程覆盖草稿/已确认/履约中取消和所有主要失败路径。
- [ ] J5. 四角色通过真实登录 token 覆盖查看/写入权限，不使用测试伪造 principal 替代全部证据。
- [ ] J6. 相同 key 重放、不同 body 冲突和并发创建最多产生一个订单；订单后续更新后再次重放仍返回创建时 snapshot v1。并发用例使用不超过 10 秒的 context/test deadline 并清理全部资源；该项证明幂等正确性，不替代 J7 的单连接池等待或 J8 的双 DB BUSY/LOCKED 证据。[test]
- [ ] J7. `TestOrderRepositoryConnectionWaitHonorsContext` 使用 channel/barrier 和不超过 10 秒的 `context.WithTimeout` 证明单 DB 连接池等待保留 canceled/deadline 错误且不写第二个订单，不使用 sleep、不映射 503；cleanup 必须释放 barrier、回滚事务、关闭 DB 并删除临时目录。[test]
- [ ] J8. `TestOrderRepositoryClassifiesBusyAcrossConnections` 或等价测试使用两个独立 DB、可注入或等价的 `<=500ms` 测试 busy timeout、不超过 10 秒的总 deadline 和 modernc SQLite 结构化错误码证明 BUSY/LOCKED→typed Unavailable；cleanup 关闭两个 DB、回滚事务并删除临时目录。[test]
- [ ] J9. 并发 Action 使用同一 version 时只成功一次，失败请求刷新后可获得新状态/version；使用不超过 10 秒的 deadline，并保证超时路径释放协调资源、回滚事务和关闭 DB。[test]
- [ ] J10. API 重启后订单、明细、version 和幂等结果保持一致。
- [ ] J11. reset/seed 后搜索结果、状态分布和详情恢复确定性基线。
- [ ] J12. `/healthz`、`/readyz`、login/me/logout 和阶段一/二测试不回归。
- [ ] J13. 配置可信 origin 后执行最小跨源 smoke：至少一个真实 OPTIONS 和一个带真实登录 token 的订单业务请求，证明 CORS transport 与订单 API 可共同工作；不要求页面 YAML、L0-L4、页面 action/reaction 或完整浏览器场景，不将该证据描述为页面联调完成。[manual-smoke]
- [ ] J14. README 删除“CORS unsupported”，记录单可信 origin、无 credentials 和显式配置限制。[doc]
- [ ] J15. `go test ./... -count=1` 通过。[test]
- [ ] J16. `go vet ./...` 通过。[test]
- [ ] J17. 使用数百条级本地 fixture 或等价轻量数据集检查默认列表和常用筛选无明显退化；推荐记录 `EXPLAIN QUERY PLAN`，也接受等价查询计划/语句计数证据，确认没有意外笛卡尔放大。本项不是延迟 SLA；access log 仍须区分成功、业务拒绝、内部错误、取消和 unavailable，且不记录 body、token 或幂等 snapshot。[review]
- [ ] J18. `go test -race ./...` 通过。[test]

证据：待记录。

## K. 文档与门禁收敛

- [ ] K1. 九个订单 endpoint 从目标 HTTP API 迁入当前 HTTP API，字段、query、header、状态码和错误码完整。
- [ ] K2. 目标 HTTP API 只保留未实现的退款、附件、看板和页面草案，不重复维护已实现订单契约。
- [ ] K3. 验证矩阵中的“订单”“幂等”两行在实施批次 A-D 期间保持 `enabled: no`；实施批次 E1 的无 CORS HTTP 和 E2 的 CORS/组合短路聚焦测试全部通过后改为 `enabled: yes`，实施批次 F 只能补充真实应用证据，不能替代 E1/E2。
- [ ] K4. 领域模型同步最终状态、能力字段、金额和 seed 语义，不把退款写成已实现。
- [ ] K5. 订单运营场景明确区分阶段三 API/CORS smoke 证据与阶段六页面门禁，并标注页面 YAML、L0-L4、页面 action/reaction、退款、附件和看板仍未实现。
- [ ] K6. 路线图将阶段三改为已实现，并链接本计划和 checklist。
- [ ] K7. README/架构代码地图同步 `internal/order` 和订单装配边界。
- [ ] K8. CHANGELOG 记录订单查询、草稿写入、履约、乐观锁、幂等和 seed，不夸大后续阶段能力。
- [x] K9. Windows 本地使用 `powershell.exe -NoProfile -File docs/audit/validate.ps1`，CI 使用 `pwsh`：扫描全部 `docs/**/*.md`，检查链接文件目标存在（外部 URL 跳过、fragment 只校验文件目标）、必需 frontmatter 和 `git diff HEAD --check`；`validate.tests.ps1` 覆盖成功与缺失链接失败 fixture，且 CI 执行 race、文档门禁和自测。
证据：2026-07-12，Windows PowerShell 5.1 执行 `powershell.exe -NoProfile -File ...` 输出 `Validated 24 Markdown files: frontmatter, relative links, and git diff HEAD --check passed.`；`validate.tests.ps1` 输出 `Validator self-test passed: valid fixture accepted, missing-link fixture rejected.`。临时 fixture 在 finally 中清理；CI 使用 `pwsh` 执行同一门禁和自测。

归档是 checklist 之外的后置动作：K1-K9 和其他工程项全部完成并记录实际证据后，先向用户提交完成报告、未执行项和剩余风险；只有用户明确确认，才移动整套 `0003` 文件并更新 `docs/audit/README.md` 与归档索引。归档确认不作为 checklist 项，也不阻塞 checklist 完成判定。
