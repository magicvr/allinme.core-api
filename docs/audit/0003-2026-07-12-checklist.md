---
status: active
owner: 后端团队
created: 2026-07-12
last_updated: 2026-07-12
applies_to: implementation roadmap phase 3 order query and fulfillment
---

# 阶段三：订单查询与履约 Checklist

配套计划：[阶段三订单查询与履约开发计划](./0003-2026-07-12-plan.md)。事实源按关注点拆分：领域规则见[领域模型](../05-domain-model.md)，阶段三详细 HTTP/CORS 契约见 plan §3-§4，endpoint 范围见[目标 HTTP API](../03-http-api-target.md)，本 checklist 只记录可观察能力和发布证据。

统一测试规范：测试名、helper 和测试 seam 均可替换；并发/连接测试设置不超过 10 秒的 deadline，并在 cleanup 释放 goroutine、channel、事务、DB、server 和临时目录。日志和失败输出不得包含 Authorization、token、密码、请求 body 或幂等 snapshot。等价实现须在对应证据中记录替代方式和理由。带 `-run` 的命令仅用于开发加速，不是硬门禁；若记录为辅助证据，必须用 `go test -json` 同时证明至少命中一个预期测试。

## P0. 开工决策

- [x] P0-1. 领域模型、plan §3-§4、目标 API、路线图、验证矩阵和 checklist 的事实源边界一致，阶段三范围不含退款、附件、看板、页面 YAML、账号管理、库存、支付网关或物流集成。
  Evidence: 2026-07-12，类型 `doc/review`；交叉复核领域模型、plan §2-§4、目标 HTTP API、实施路线图、验证矩阵和本 checklist。领域模型持有状态机、角色、金额与业务不变量，活跃 plan 持有详细 HTTP/幂等/CORS 契约，目标 API 持有 endpoint 范围，路线图与验证矩阵持有交付及启用时点。目标 API §9 已区分阶段三 endpoint 收敛与阶段六页面场景门禁，避免 R1/R3/R6 与“页面未完成不阻塞阶段三”冲突；未发现剩余范围或事实源边界冲突。
- [x] P0-2. 冻结金额、分页、状态机、角色、错误码、幂等 snapshot 和 CORS 外部契约；实现调整不得绕过对应事实源。
  Evidence: 2026-07-12，类型 `doc/review`；交叉复核并统一金额、分页、订单状态机、角色读写矩阵、错误码、create 幂等作用域与不可变 snapshot v1、CORS 行为和中间件顺序。create 不接受客户端 `version` 并由服务端初始化为 `1`，edit/Action 使用当前 `version`；未发现剩余实质冲突。
- [x] P0-3a. schema 采用 additive v3/v4 和 roll-forward 策略；已按 plan §8 的 schema-only 发布 Evidence 模板分别冻结 v3、v4 的目标环境、产物保存规则、构建与启动命令模板、路由关闭机制、配置与数据库路径、停写备份与恢复步骤、回退触发条件和数据丢失边界。
  Evidence: 2026-07-12，类型 `doc/review`；目标明确限定为单进程本地 demo 与 CI 临时目录演练，不声称共享测试或生产部署能力。plan §8 已冻结 v3/v4 共用的固定 API/admin 产物构建、`artifacts/phase3/<schema>/<revision>/` 保存与 SHA-256 模板，M1-A/M2-B0 由编译进产物的 route registration 状态加真实 404 smoke 保证关闭路由，并冻结停写后主库/WAL/SHM 集合备份、整库恢复、回退触发条件和恢复点后写入丢失口径。本项不填写尚未实现的 revision/hash 或演练结果；实际值由 M1-1/M2-3 记录。
- [x] P0-4. 记录普通测试、vet、非缓存 race 和 Git revision 的开工基线。
  Evidence: 2026-07-12，Go `go1.26.0 windows/amd64`；`code_baseline_revision=5648736914239987eff00cb146478d1db082e5d9`（最后一次影响代码的 revision 之前的文档刷新点；其后至审视时仅有审计文档提交）；`reviewed_head_revision=021f25029f0d89b6b39fca2aaa3be501326fb2ff`（审视时 HEAD）。在 `code_baseline_revision` 上记录的命令结果：`go test ./... -count=1` exit 0，6.896s；`go vet ./...` exit 0，0.429s；`go test -race ./... -count=1` exit 0，58.503s，其中 `internal/store` 53.953s、`internal/app` 42.069s、`internal/auth` 42.135s。后续纯文档提交不改变代码基线，除非再次影响 `*.go` 或依赖清单。
- [x] P0-5. 文档 validator 和 validator 自测可在 Windows PowerShell 与 CI pwsh 执行。
  Evidence: 2026-07-12，Windows 当前用户执行策略为 `RemoteSigned`，使用 `powershell.exe -NoProfile -ExecutionPolicy Bypass -File docs/audit/validate.ps1` 与 `.../validate.tests.ps1`；`validate.ps1` 验证 24 个 Markdown 文件通过；`validate.tests.ps1` 合法 fixture 成功、缺失链接 fixture 失败；CI 已配置相同门禁。plan §8 已记录该 Windows 环境前置条件。

具体 owner、reviewer、替补、日期和升级路径由项目管理系统维护，不作为产品正确性或发布证据。

## M1. 只读订单 API

M1 按 plan §8 的 M1-A / M1-B gate 执行。M1-A 覆盖 M1-2 → M1-1/repository contract → M1-3 → schema-only smoke；推荐实现顺序为领域类型与纯测试、migration v3 与 repository contract、deterministic seed/reset、schema-only 构建与 smoke。M1-A 通过前不得注册订单 HTTP 路由、不得创建 v4 `idempotency_keys`、不得实现金额计算或履约状态转换。M1-B 覆盖 M1-4/M1-5/M1-6 的只读 list/detail HTTP，并在 M1-7 启用验证矩阵“订单查询”。

- [x] M1-1. additive migration v3 只创建 orders/order_items 和必要查询索引，并建立只读 repository contract 与对应纯测试；schema-only binary 的失败事务回滚、重复执行、v2→v3 数据保留、v3 readiness 和无订单路由 smoke 通过，不创建 idempotency 表；完成后它成为 v3 数据库的回退产物。
  Evidence: 2026-07-12，类型 `test/smoke`；最终代码 revision `5c84c47030ef831e4d042398ffb66e97187397c9`，固定产物目录 `artifacts/phase3/v3/5c84c47030ef831e4d042398ffb66e97187397c9/`；`api.exe` SHA-256 `B50A234577F81E1997E906B957AA65D78C37622D2B81D9F831A59DF91D080EA8`，`admin.exe` SHA-256 `2D18E601A047E6FC25321C848997E8AFCBEAD9B8824F04016CFF1B4D01FD2470`。固定 admin 产物验证 migration `0→3`、重复 `3→3`、runtime/auth/order seed 首次及只读重放；固定 API 产物验证 `/readyz=200`、GET/POST `/api/v1/orders=404`。停机后的主库/WAL/SHM 集合备份 hash 分别为 `7094F67B80F2F85D2C02B683E84CB7B52DEB7C0DFE49730D84BDC73FB00C801B`、`E3B0C44298FC1C149AFBF4C8996FB92427AE41E4649B934CA495991B7852B855`、`FD4C9FDA9CD3F9AE7C962B0DDF37232294D55580E1AA165AA06129B8549389EB`；复制到新的空 restore-data 后，同一固定 API `/readyz=200` 且订单 GET 仍为 404，原库未删除或覆盖。自动化测试覆盖 v3 失败时保持 `user_version=2` 且不残留 `order_items`、v2 auth 数据保留和无 `idempotency_keys`。
- [x] M1-2. `internal/order` 的读模型/status 枚举、读取权限、capability、ID 和 clock 行为通过纯领域测试；金额计算与状态转换不作为 M1 阻断项。
  Evidence: 2026-07-12，类型 `test`；新增 `internal/order` 的订单/支付状态、读模型、窄 `Repository` contract、四角色读取权限、阶段三 capability、`ord_`/`itm_` 128-bit ID 格式和 UTC clock/RFC3339 行为。`go test ./internal/order -count=1` 通过；repository compile-time stub 证明 contract 不暴露 SQL/HTTP 类型。未实现金额计算或状态转换。
- [x] M1-3. development seed/reset 建立确定性订单基线且可重复校验，production 不写演示订单。
  Evidence: 2026-07-12，类型 `test`；migration v3 后 `order_demo=1` 创建六个固定订单/明细和冻结时间/状态/支付组合，重复 seed 只读校验且允许额外业务订单存在，篡改/缺失/冲突均失败且不静默修复；development admin 固定按 runtime→auth_demo→order_demo 执行，reset 恢复六订单，production seed 保持零订单。`go test ./internal/store ./internal/admin -count=1` 通过。
- [x] M1-4. 列表支持冻结的搜索、筛选、日期、分页和稳定 allowlist 排序；COUNT/page 位于同一 SQLite snapshot；store 内部 query observer 对 0、1、数百条 fixture 均断言恰好两次查询执行，无逐订单或第三条补查询。
  Evidence: 2026-07-12，类型 `test`；`internal/store/orders.go` 使用显式只读事务执行一次 COUNT 和一次 page query，测试内 `queryObserver` 只统计这两次执行。`TestOrderRepositoryListUsesTwoQueriesAndStableFiltering` 覆盖空匹配、单条匹配和 306 条总 fixture/100 条当前页，三种规模均断言恰好两次查询；同时覆盖 q 对 ID/customer/SKU/item 的参数化匹配、LIKE 特殊字符字面量、日期筛选、allowlist 排序和列表不加载 items。命令 `go test ./internal/store -count=1` 通过。
- [x] M1-5. 详情按 position 返回明细；非法/不存在 ID、取消、关闭 DB、扫描失败和损坏数据得到冻结错误且不泄露 SQL/驱动细节；扫描失败由 store 内部测试 stub 注入，损坏数据由临时数据库启用 `ignore_check_constraints` 后直接插入并恢复约束，不扩大生产接口或修改正式 migration。
  Evidence: 2026-07-12，类型 `test`；详情在只读事务内读取订单和 `ORDER BY position` 明细，`TestOrderRepositoryDetailAndCorruptData` 覆盖存在/不存在、关闭 DB 和绕过 CHECK 写入非法 status；`TestScanOrderFailureInjection` 通过包内 `scannerStub` 注入扫描失败。service 保留 context cancel/deadline 的 `errors.Is` 链，HTTP 将非法/不存在 ID 统一为 `404 NOT_FOUND`，内部错误统一为安全 `500 INTERNAL_ERROR`。命令 `go test ./internal/order ./internal/store ./internal/httpapi -count=1` 通过。
- [x] M1-6. 认证 app 使用共享 DB 装配只读订单路由；四角色真实 token 可查询稳定 DTO，基础 app 和未注册写路由保持 404，route metadata 驱动 mux/Allow/HEAD/known path。
  Evidence: 2026-07-12，类型 `test/smoke`；authenticated app 在共享 `store.DB` 上创建 order service，基础 `NewAPI` 不装配订单依赖。`TestAuthenticatedAPIFlowWithSQLite` 使用 deterministic auth/order seed 对 viewer/operator/approver/admin 分别真实登录并查询列表，验证详情 items、capability、HEAD `405 Allow: GET`，以及尚未启用的 POST/PATCH 为 404；`internal/httpapi/orders_test.go` 覆盖 DTO、query、非法 ID 和 route gate。命令 `go test ./internal/httpapi ./internal/app -count=1` 通过。
- [x] M1-7. M1 硬门禁 `go test ./internal/order ./internal/store ./internal/admin ./internal/httpapi ./internal/app -count=1` 与 `go test -race ./internal/order ./internal/store ./internal/admin ./internal/httpapi ./internal/app -count=1` 通过，关闭订单路由后 v3 数据库仍可用，并在同一变更中将验证矩阵“订单查询”改为 `enabled: yes`。
  Evidence: 2026-07-12，类型 `test/review`；普通门禁通过，总耗时 `9.19s`；race 门禁通过，总耗时 `66.8s`，各包均为 `ok`。基础 app 继续在 v3 数据库上只提供 health/readiness/fallback，订单 path 为 404；authenticated app 启用两个只读 GET，未注册写路由保持 404。验证矩阵“订单查询”已改为 `enabled: yes`，两个 GET endpoint 已从目标 API 迁入当前 API。

## M2. 完整草稿写入与幂等

- [ ] M2-1. create/edit typed command 完成字符串、items、CNY、整数词法、数量、单价、总额和溢出校验，服务端不接受客户端总额。
  Evidence: 待记录，类型 `test`。
- [ ] M2-2. M2-A 仅在 service/store 内完成 create/edit command、金额、聚合事务和 version；真实 app 的负向测试证明 `POST /api/v1/orders` 和 `PATCH /api/v1/orders/{orderId}` 返回 404，且 M1 查询仍可用。
  Evidence: 待记录，类型 `test/smoke`；M2-A 门禁为 `go test ./internal/order ./internal/store ./internal/app -count=1` 与对应 `-race` 命令，记录测试名、执行环境和耗时。
- [ ] M2-3. M2-B0 additive migration v4 只创建 idempotency_keys 及索引；schema-only binary 的 v3→v4 数据保留、失败回滚、重复执行、v4 readiness 和 M1 查询 smoke 通过，POST/PATCH 仍为 404；完成后它成为 v4 数据库关闭写能力的回退产物。
  Evidence: 待记录，类型 `test/smoke`；迁移 v4 前重新复核 P0-3a 的 v4 部署与恢复步骤，并记录实际 revision、固定产物标识/hash、构建与启动命令、迁移及回退演练结果；证据通过后才允许 M2-B1 注册写路由，不得回退到 v3 binary。
- [ ] M2-4. 幂等作用域、normalized digest 和不可变 snapshot v1 符合 plan；replay 不读取当前订单，损坏/未知 snapshot 安全失败且不重新创建。
  Evidence: 待记录，类型 `test`。
- [ ] M2-5. 两个连接竞争同一 key 最多创建一个订单和一份 snapshot；单连接池取消与双 DB BUSY/LOCKED 分类分别有自动化证据。
  Evidence: 待记录，类型 `test`。
- [ ] M2-6. 编辑仅允许当前 version 的 DRAFT，在事务中重写明细/总额并递增 version；成功、失败和 replay 的 createdAt/updatedAt 行为稳定。
  Evidence: 待记录，类型 `test`。
- [ ] M2-7. M2-B1 一次性注册完整 create/edit：operator/admin 的真实 token 可创建、编辑、重放并观察冲突，viewer/approver 被拒绝且数据不变；不得存在无幂等 create 或提前暴露 edit 的可启动状态。
  Evidence: 待记录，类型 `test/smoke`。
- [ ] M2-8. M2 硬门禁 `go test ./internal/order ./internal/store ./internal/httpapi ./internal/app -count=1` 与 `go test -race ./internal/order ./internal/store ./internal/httpapi ./internal/app -count=1` 通过，回退到 M2-B0 后 v4 数据库与 M1 查询仍可用，并在同一变更中将验证矩阵“订单写入/幂等”改为 `enabled: yes`。
  Evidence: 待记录，类型 `test/review`；记录两条命令和耗时。

## M3. 履约与浏览器 Transport

- [ ] M3-1. confirm/fulfill/ship/complete/cancel 只允许领域模型中的源状态和 operator/admin，全部成功路径返回递增 version 的当前订单。
  Evidence: 待记录，类型 `test`。
- [ ] M3-2. 条件更新未命中按不存在→version→状态分类；同一 version 的并发 Action 最多一个成功且不重复改变状态。
  Evidence: 待记录，类型 `test`。
- [ ] M3-3. Action HTTP 严格处理 Content-Type、body、整数词法、范围、version、状态和权限，稳定映射 400/403/409/415/422。
  Evidence: 待记录，类型 `test`。
- [ ] M3-4. M3-A 集成 gate 覆盖状态转换函数、五个 Action 的合法/非法转换、真实 token 和并发冲突；真实 app 回退测试证明关闭 Action 路由后 create/edit 仍可用、五个 Action 均为 404，并在同一变更中将验证矩阵“订单履约 Action”改为 `enabled: yes`。
  Evidence: 待记录，类型 `test/smoke`；门禁为 `go test ./internal/order ./internal/store ./internal/httpapi ./internal/app -count=1` 与对应 `-race` 命令，记录测试名、执行环境和耗时。
- [ ] M3-5. `config.LoadAPI` 在 app 构造前拒绝非法 origin；actual/preflight、Vary、Expose-Headers、Max-Age、known path 和组合短路与 plan §4 一致。
  Evidence: 待记录，类型 `test`。
- [ ] M3-6. 请求取消使用连接级自动断言且不写业务 envelope；Unavailable 映射 503 + Retry-After；访问日志以结构化 `outcome=completed|canceled|unavailable` 区分结果，未提交的 canceled 请求省略 `status`，已提交响应记录实际 status，且不记录敏感内容。
  Evidence: 待记录，类型 `test/review`；按 slog attribute 断言，不依赖日志 message 文案。
- [ ] M3-7. 真实 app 的跨源 OPTIONS + 认证订单请求、重启持久化、reset/seed 和阶段一/二回归均通过自动化测试。
  Evidence: 待记录，类型 `test/smoke`。
- [ ] M3-8. M3-B 硬门禁 `go test ./internal/config ./internal/order ./internal/store ./internal/httpapi ./internal/app ./internal/admin -count=1` 与 `go test -race ./internal/config ./internal/order ./internal/store ./internal/httpapi ./internal/app ./internal/admin -count=1` 通过；禁用 CORS 配置后 M3-A API 保持可用，并在同一变更中将验证矩阵“CORS”改为 `enabled: yes`。
  Evidence: 待记录，类型 `test/review`；记录两条命令和耗时。

## R. 文档与发布门禁

- [ ] R1. 只有对应代码和集成测试完成后，订单 endpoint 才从目标 API 迁入当前 API；目标 API 删除已实现字段副本。阶段三迁入只要求真实 HTTP、业务、权限、并发和稳定契约测试，不要求阶段六 Schema-UI 页面场景。
  Evidence: 待记录，类型 `doc/review`。
- [ ] R2. 复核验证矩阵四项状态与实际命令一致：查询 `M1-B`、写入/幂等 `M2-B1`、履约 `M3-A`、CORS `M3-B`；本项不承担首次启用。
  Evidence: 待记录，类型 `doc/test`。
- [ ] R3. 领域模型、架构、路线图、README、场景和 CHANGELOG 同步最终能力与非目标，不提前宣称页面能力；页面 YAML/action/reaction/完整 Schema-UI 场景归属阶段六，不阻塞阶段三归档。
  Evidence: 待记录，类型 `doc/review`。
- [ ] R4. `go test ./... -count=1`、`go vet ./...` 和独立 `go test -race ./... -count=1` 通过，且未通过放宽 deadline 掩盖超时；记录现有全局 `test`/`race` CI job 耗时，各 gate 命令耗时只作诊断基线，不设额外通过阈值。
  Evidence: 待记录，类型 `test`。
- [ ] R5. `validate.ps1`、`validate.tests.ps1` 和 `git diff HEAD --check` 通过；金额、错误码、CORS、snapshot 或 endpoint 变更已完成跨文档事实源检查。
  Evidence: 待记录，类型 `doc/test`。
- [ ] R6. 提交完成报告并记录剩余风险；只有用户确认后才归档计划/checklist，归档不是本 checklist 的完成项。
  Evidence: 待记录，类型 `review`。

非阻断观察：可使用数百条级 fixture、`EXPLAIN QUERY PLAN` 或等价语句计数观察列表查询计划；该观察不是延迟 SLA，不影响阶段三完成判定。
