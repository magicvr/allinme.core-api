name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout core API
        uses: actions/checkout@v4

      - name: Checkout protocol fixtures
        uses: actions/checkout@v4
        with:
          repository: magicvr/schema-ui-docs
          ref: 02a0528cd49d21cbb345efc2c8a741fabb71fd47
          path: schema-ui-docs

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Test
        env:
          SCHEMA_UI_FIXTURES: ${{ github.workspace }}/schema-ui-docs/conformance/fixtures
        run: go test ./...

      - name: Vet
        run: go vet ./...